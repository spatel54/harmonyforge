# Plan

### How to use this doc

Skim **Status at a glance** below, then use the numbered **Plan** checklist for implementation detail. **Living narrative** (session notes, deep dives, regressions) lives in **[progress.md](progress.md)** — **one-screen digest:** **[Last updated (2026-04-27 — naturals keyboard, LLM env, audit copy)](progress.md#last-updated-2026-04-27-inspector-sandbox)** (keyboard **↑/↓** = diatonic **naturals**; **Salamander** transpose preview; **`OPENAI_*` swap** recovery + **`configHint`**; lay **SATB audit** line) · **[Earlier 2026-04-27 (flush & toolbar)](progress.md#last-updated-2026-04-27)** (flush-aware **`transposeNotes`**; **`onToolbarAction` → `handleToolSelect`**; **`getTransposeTargetNoteIds`**; Advanced hidden; light theme); then **[Last updated (2026-04-24)](progress.md#last-updated-2026-04-24)** (cross-surface copy, team narrative, Playground **♫** aureole); then **[Last updated (2026-04-23)](progress.md#last-updated-2026-04-23)**; **UX + team tranche:** **[Work log — Cross-surface UX, team narrative & Playground stand (2026-04-24)](progress.md#wl-ux-team-playground-2026-04-24)**; **Team / credits (baseline):** **[Work log — Team page & global credits (2026-04-23)](progress.md#wl-team-credits-2026-04-23)** (`TeamNavButton`, `/team`, `public/creators` headshots); **Iteration 6:** **[Work log — Iteration 6 (2026-04-23)](progress.md#wl-iteration-6-2026-04-23)** (pedal bass, inspector float/dock, prompts, localized bar harmony API); **Sandbox fluidity / “clunky UI”:** **[Work log — Sandbox fluidity… (2026-04-23)](progress.md#wl-sandbox-fluidity-2026-04-23)** (multi-select pitch, inspector float **resize**, **`hf-pressable`** + modal motion, action bar removed); **full work log:** **[Sandbox & export polish (2026-04-23)](progress.md#wl-sandbox-ux-polish-2026-04-23)** (**rest ghost** hardening + **`NaN`** guards + hotkeys dialog/`e.code`, **export** paper + LIGHT **`presentation`**, **RiffScore↔HF multi-select**, **viola** — **`hfClefToRs`** + **RiffScore `getOffsetForPitch` / `getPitchForOffset`** fix for **alto/tenor** staves, **`index.js`/`index.mjs` `toolbarPlugins`** parity, **`pendingClefChange`** runtime notes). For the **2026-04-22** MVP + Inspector bundle, **[Program narrative (2026-04-22)](progress.md#program-narrative-2026-04-22)**; **rest hover** origin story — **[Sandbox score UX](progress.md#wl-sandbox-score-ux-2026-04-22)** (**hover** rest → **ghost**; **click** commits **duration/dots**; paste/insert **replace** rest slots). The **[2026-04-20 narrative](progress.md#program-narrative-2026-04-20)** remains useful for older consolidated tables. **2026-04-22 MVP slice:** Theory Inspector **Explanation \| Chat**, **chat tags**, fixed **beginner** tutor depth (no B/S/P toggle), **suggested chats above composer**, **pickup 0–3** + optional **infer chords** on Document, engine **`pickupBeats`**, **`localStorage`** generation config, sandbox **reset baseline** + **first-visit overlay** (`hf_onboarding_seen`), **VoiceDropdown** grouping + SVGs, **progression window** FACTs — **[Work log — MVP phase ship…](progress.md#wl-mvp-phase-ship-2026-04-22)**. **2026-04-21 Document / Ensemble UI:** collapsible **Glass Box** pedagogy + **always-visible** algorithms-vs-AI line — **[Work log — Ensemble Builder UI…](progress.md#wl-ensemble-builder-ui-2026-04-21)**. **2026-04-20 LLM env:** **`getServerOpenAIEnv()`**; **`OPENAI_API_KEY`** alone suffices for default OpenAI ( **`OPENAI_BASE_URL`** optional) — **[Work log — LLM env…](progress.md#wl-llm-env-2026-04-20)**. **Architecture:** **[context/system-map.md](context/system-map.md)**. **Doc index:** **[docs/README.md](README.md)**. **Deploy:** **[deployment.md](deployment.md)**.

### Status at a glance

- **Sandbox naturals transpose + LLM env + audit copy (2026-04-27):** **[progress.md — work log](progress.md#wl-sandbox-naturals-llm-audit-2026-04-27)** — With note(s) selected, **keyboard ↑/↓** moves by **diatonic white-key steps** (**natural** pitch strings only); **⌘/Ctrl+↑/↓** = octave then natural spelling; **toolbar / F9 palette** keep **±2 semitone** chromatic transpose (**`transposeNotes`**). **`sandboxPitchPreview`** uses **Salamander** (**`Tone.Sampler`**) like RiffScore clicks. **`getServerOpenAIEnv`** auto-**swaps** **`OPENAI_API_KEY`** ↔ **`OPENAI_MODEL`** when the user pasted model id into the key slot (**401** shows **`gpt-4o-mini`**); **`GET /api/theory-inspector`** returns **`configHint`**. **SATB audit** chat message is one short **red vs blue** line. **`make test`:** **296** Vitest when last gated. **Open:** manual QA; **toolbar `Octave ↓`** ([Iteration 7 follow-up](progress.md#wl-iteration-7-followup-2026-04-25-pm)); Vercel **Preview** env + **redeploy** after changing secrets.
- **Cross-surface UX + team narrative + Playground stand (2026-04-24):** **[progress.md — 2026-04-24](progress.md#wl-ux-team-playground-2026-04-24)** — Unified copy across **`/`**, **`/document`**, **`/sandbox`**, **`/team`** (**Configure** step, **Harmony setup**, footer **Team** link, onboarding slides, Glass Box blurbs, audio banner); **team** page collapsible profiles, “how we work together” placement, stack dedupe; **Playground** upload prompt wrap-friendly type, **music-stand** hover pulse + **♫**-shaped glow (no rectangular hover card). **Vitest:** **273** passing. **Open:** verify **♫** glyph on minimal font stacks; program backlog still **Iteration 6** below.
- **Team page & credits (2026-04-23):** **[progress.md — Team page](progress.md#wl-team-credits-2026-04-23)** — Global **`TeamNavButton`** (`Users` icon) on **`DocumentHeader`** / **`SandboxHeader`** → **`/team`**; creator bios, technical blurbs (backend **Cursor** / frontend **Claude** + context engineering), quotes; headshots in **`frontend/public/creators/`** (commit for deploy). **Open (program):** not blocked by this slice—same Iteration 6 backlog below.
- **Sandbox editor reliability (2026-04-27):** **[progress.md — 2026-04-27 work log](progress.md#wl-sandbox-reliability-2026-04-27)** — **`onToolbarAction` → `handleToolbarAction` → `handleToolSelect`** (toolbar aligns with F9 palette; **`return true`** avoids double **`applyOnSelection`**); post-flush **`useScoreStore.getState().score`** for **`transposeNotes`** after **`getTransposeTargetNoteIds`** / palette pitch paths; **`onToolbarPrint`** / score-only PRN; **Advanced** pickup / infer-chords UI removed from **`EnsembleBuilderPanel`**. **`make test`:** **296** Vitest when last gated (includes **`llmClient.test.ts`**). **Open:** manual QA on arrows + toolbar **`Octave ↓`** (re-test after unified path); **Iteration 7 residual:** see **[progress.md — Iteration 7 follow-up](progress.md#wl-iteration-7-followup-2026-04-25-pm)** if **`8−`** still fails in browser.
- **Iteration 7 (2026-04-25):** **[progress.md — Iteration 7](progress.md#wl-iteration-7-2026-04-25)** + **[follow-up](progress.md#wl-iteration-7-followup-2026-04-25-pm)** — unified pitch keys; Theory Inspector alternatives + rhythm guard; concise Harmony setup copy; Team footer new-tab behavior; Playground click-burst polish; Theory Inspector explanation empty-state prompt; preview scrollbar fixes; timbre preview UI removed pending future revisit. **Historical failure:** toolbar **`Octave ↓`** intermittent vs hotkeys — **2026-04-27** improvements: **`getTransposeTargetNoteIds`**, post-flush **`useScoreStore`** transpose, **`onToolbarAction` → `handleToolSelect`** (toolbar matches keyboard/palette). **Re-verify in browser** before closing.
- **Iteration 6 (2026-04-23):** **[progress.md — Iteration 6](progress.md#wl-iteration-6-2026-04-23)** — **`bassRhythmMode`** pedal vs follow in **`satbToMusicXML`** + Document/Ensemble UI; **Theory Inspector** **Guided/Concise** + **`prompts.ts`** brevity + stylist **duration** options; Sandbox inspector **Dock/Float** + **Alt+bar** measure note select + **multi-select** chat FACT; **`POST /api/generate-harmony-range`** + client **`spliceHarmonyMeasuresFromAddonScore`**; expanded **VOICE_INSTRUMENTS**; Document **responsive** two-column stack. **Open:** range-merge when part counts differ from config; pickup / multi-bar ranges; touch has no Alt-hover affordance.
- **Sandbox fluidity & UI polish (2026-04-23 late):** **[progress.md — Sandbox fluidity](progress.md#wl-sandbox-fluidity-2026-04-23)** — **multi-select pitch** (arrow/drag moves full selection via **`pitchGroupRef`** + **`useRiffScoreSync`**); **floating inspector resize** + persisted layout; **`SandboxActionBar`** removed; **`globals.css`** motion/press tokens (**`hf-pressable`**, modal/toast/banner); softer **Theory Inspector** chrome; **StepBar** / **AudioUnlockBanner** / sandbox toast polish; export modal keeps **stable** fixed layout (responsive stack deferred). **Open:** localized range merge; touch/Alt gaps; optional narrow-viewport export; manual QA. **Vitest:** suite grew to **273** by **2026-04-24** (see **[progress.md — 2026-04-24](progress.md#last-updated-2026-04-24)**); no new gate failure from the UX tranche.
- **Sandbox & export polish (2026-04-23):** **[progress.md — Work log 2026-04-23](progress.md#wl-sandbox-ux-polish-2026-04-23)** — **rest-hover ghost** layout/`NaN` hardening + stable effect deps, **`SandboxHotkeysDialog`** + **`sandbox/page.tsx`** key parity (**Caps Lock**, **`e.code`**, numpad), **export** score-on-paper **`#F8F3EA`** + **`presentation` LIGHT** + PNG match, **multi-select** bridge (**`mapRiffSelectedNotesToHFSelections`**, **`onEditorSelectionChange`**, ⌘A/Esc), **viola / alto & tenor staves** — **`hfClefToRs`** (no **`PART_CLEF_MAP`** SATB **alto voice** → treble bug) **plus** **`patches/riffscore+1.0.0-alpha.9.patch`** layout fix (**`getOffsetForPitch`** / **`getPitchForOffset`** must not use treble offsets for **alto/tenor**), **CJS `index.js`** **`toolbarPlugins`** parity with **ESM `index.mjs`** (avoids stale **`pendingClefChange`** / dual-bundle issues); after pull run **`cd frontend && rm -rf .next && npm install`**. **Open:** dense scores/chords, **Inspector overlay** vs native selection drift, **touch** (no hover), manual **viola** spot-check.
- **MVP + Inspector polish (2026-04-22):** **[progress.md — Program narrative 2026-04-22](progress.md#program-narrative-2026-04-22)** — Document **Advanced** (pickup, infer chords), engine **`pickupBeats`**, **`localStorage`** **`useGenerationConfigStore`**, Inspector **tabs / tags / beginner-only depth / suggested chats above input**, sandbox **reset + `hf_onboarding_seen`**, **VoiceDropdown** groups, **progression FACT** window, **`usePlayback`** hardening. **Sandbox score UX (same tranche, late):** **[progress.md — Sandbox score UX](progress.md#wl-sandbox-score-ux-2026-04-22)** — **rest hover ghost** (pitch from **`pitchAtStaffVerticalInContainer`**), commit uses **rest duration/dots**, **paste/insert** replace-on-rest, **hotkeys** + **`isTypingTarget`**, **`ConfigurationBackFAB`**, **StepBar** full labels, reset **re-hydrate**. **`npm run lint`** **exits 0**; **warnings** — **`@next/next/no-img-element`** (**`VoiceDropdown`**, **`RiffScoreEditor`**), unused **`_e`** param. **Open:** manual QA for hover ghost (**touch** = no hover; dense scores; Inspector mode disables ghost).
- **Ensemble Builder UI (2026-04-21):** Document **`EnsembleBuilderPanel`** — **always-visible** algorithms-vs-AI line; collapsible **`GlassBoxPedagogyCallout`**; **Sound & style** + **4-part harmony** instrument card — **[progress.md — Ensemble Builder UI](progress.md#wl-ensemble-builder-ui-2026-04-21)**. **RiffScoreEditor** hook-order issue **resolved (2026-04-22)**.
- **Theory Inspector — OpenAI-compatible env (2026-04-20; swap recovery 2026-04-27):** Server reads **`OPENAI_API_KEY`**, optional **`OPENAI_BASE_URL`** / **`OPENAI_URL`**, and **`OPENAI_MODEL`** via **`getServerOpenAIEnv()`** in `frontend/src/lib/ai/llmClient.ts`. **`frontend/.env.example`** documents all three (default model when unset: **`gpt-5-nano`**). **`GET /api/theory-inspector`** returns **`hasApiKey`**, **`hasCustomBaseUrl`**, and **`configHint`** when env looks misconfigured (**swapped **`OPENAI_API_KEY`** ↔ **`OPENAI_MODEL`** or model string in key slot**) — **[progress.md — naturals / LLM / audit](progress.md#wl-sandbox-naturals-llm-audit-2026-04-27)**. Use **`OPENAI_BASE_URL`**, not a generic **`BASE_URL`**, for gateways. Participant-facing gaps (**progression-aware** tutor, **expressive sovereignty**) are tracked in **[progress.md — Iteration 3](progress.md#program-narrative-2026-04-20)** and **[Iteration3.txt](Iteration3.txt)**. **(2026-04-20 late)** **Localized bar regenerate** and **genre/jazz-pop** Document controls were **removed**; **generate** is **classical-only** from the UI — see **[progress.md — Learner note names…](progress.md#wl-learner-notes-scope-2026-04-20)**.
- **Learner pitch labels (2026-04-20):** Optional **letter + accidental** above noteheads; RiffScore **testid** mapping, **`getRsToHf()`**, rest-aware walks; overlay **z-order**, **toolbar clip**, FAB/palette **z-50**; exports unchanged. **[progress.md — Label refinement…](progress.md#wl-learner-pitch-labels-refine-2026-04-20)** (scope + classical-only: **[Learner note names…](progress.md#wl-learner-notes-scope-2026-04-20)**).
- **Document UX — chord gating, playback, pedagogy, tooltips (2026-04-20):** RiffScore **chord track / symbols** only when **`parts.length >= 3`** (`shouldShowChordNotation` in `riffscoreAdapter.ts`; CSS safety net in `RiffScoreEditor`). **`/document`:** `AudioUnlockBanner`, floating **Play** on preview with `Tone.start`, tighter **Glass Box** + **(i)** copy; **`Tooltip`** explicit width fixes unreadable narrow popovers. Tests: **`riffscoreAdapter.test.ts`**. See **[progress.md — Work log — Document UX…](progress.md#wl-document-ux-2026-04-20)**.
- **Consolidation + PDF hardening (2026-04-19):** Backend + frontend merged into a single Next.js app. The SATB engine now lives at `frontend/src/server/engine/*` and is called directly by route handlers in `frontend/src/app/api/*` — no more Express proxy on `:8000`, no `NEXT_PUBLIC_API_URL`. Client rasterizes PDFs via `pdfjs-dist` (hook `useClientPdfPreview`) so Document always shows a preview; the engine now accepts pre-rasterized PNGs and merges multi-page PDFs (`mergeParsedScores`), closing §1.9m's "page 1 only" footnote. A root `Dockerfile` bundles Node + Next + pdfalto + Poppler + Python/oemer for self-hosted PDF OMR. Lint is green (0 warnings); frontend **`npm test`** was **223** Vitest tests as of 2026-04-20 (run **`make verify`** for full gate). Theory Inspector auto-wires tutor `<<<INTENT>>>` JSON to store setters + `router.push` (mood/genre/pickup/regenerate/navigate) via `intentRouter.ts`. `ideaActionResolve` gained `staffIndex` disambiguation + nearest-in-measure fallback. ADR 003 vertical slice shipped — Bb clarinet / F horn / Eb alto sax emit proper `<transpose>` in MusicXML. Audio unlock banner on Sandbox mitigates "inaudible first note" Iter2 §4; Document uses the same banner pattern for preview playback.
- **Study refinement (2026-04-18):** Comprehensive pass addressing friction captured in **[iteration1.txt](iteration1.txt)** / **[Iteration2.txt](Iteration2.txt)** — tenor clef fix, rhythm density (`chordal`/`mixed`/`flowing`), voice-independence bias, anacrusis auto-detection, persistent Document config, Inspector starter prompts + musical goal + honest INTENT block, sandbox delete-to-rest, labeled toolbar, volume slider, playback resilience. See [progress.md — Work log — Iteration 1+2 study refinement (2026-04-18)](progress.md#wl-study-refinement-2026-04-18).
- **Glass Box pedagogy copy (2026-04-19):** In-app callouts on Document (`EnsembleBuilderPanel`) and Theory Inspector distinguish **deterministic harmony generation** from **conversational AI** (tutor/critic/suggester). See [progress.md — Glass Box pedagogy callouts](progress.md#wl-glass-box-pedagogy-2026-04-19).
- **Logic Core (engine in Next):** Items **1.1–1.9** shipped, including **1.9n** symbolic intake (2026-04-07) and **1.9m** PDF/OMR path (2026-04-19 — client preview + Docker/server OMR; Vercel serverless has no bundled oemer by design).
- **Tactile Sandbox (frontend):** **RiffScore-first** editing — `EditableScore` in Zustand is the canonical score model; **`patch-package`** extends RiffScore (toolbar plugins, playback scrub via `__HF_RIFFSCORE_PLAY_FROM`). VexFlow/OSMD remain in the tree for legacy paths and fallbacks where noted in [progress.md](progress.md).
- **Flow:** Upload → Document → Generate → Sandbox; optional **`/onboarding`** and **6-step** product **tour** (`CoachmarkOverlay`, `data-coachmark="step-1"`…`step-6`, persist **`hf-coachmarks-v2`** + **`hasDismissed`**). See [progress.md — 6-step tour](progress.md#wl-coachmarks-6step-2026-04-07).
- **Theory Inspector:** `/api/theory-inspector` and `/suggest`; dual mode; **fixed tutor depth beginner** (thorough, concise; **no** user-facing B/S/P control — **[2026-04-22 narrative](progress.md#program-narrative-2026-04-22)**); **Explanation \| Chat** tabs; chat **tag strip**; notation-first FACT blocks + progression window; SATB audit posts a **very short** system line (**red = stronger flag, blue = lighter** — **[2026-04-27](progress.md#wl-sandbox-naturals-llm-audit-2026-04-27)**) plus **highlights**. **`<<<IDEA_ACTIONS>>>`**, **`ideaActionResolve`**, ghost labels — [Work log — Theory Inspector…](progress.md#wl-inspector-split-ideas-2026-04-06).
- **M5 (user study):** RQ1 (`reviewer_primary` / melody-only) and RQ2 (`minimal` stylist prose) plus opt-in logging — see **M5 — User study** below.
- **Sandbox parity — palettes, rest repitch, clean export (2026-04-20):** **MuseScore/Noteflight-style palette panel** docked right of the score (Clefs, Key/Time Signatures, Barlines, Accidentals, Articulations & Ornaments, Dynamics, Lines, Repeats & Jumps, Tempo, Text, Tuplets, Breaths) — see §2g.10. **Rest → note re-pitch** restores a rest as a note of the same duration when you type A–G on it or at the cursor — §2g.9. **(2026-04-22 late)** **Hover** a rest (sandbox) to preview pitch and **click** the ghost to replace **without** picking a duration tool first; commit uses the rest’s **duration/dots** — see §2g.9 and **[progress.md — Sandbox score UX](progress.md#wl-sandbox-score-ux-2026-04-22)**. **Export / print** now renders a dedicated `ExportPrintRoot` so PDF and PNG output contain only the score, never the toolbar/palette/inspector chrome.
- **Active gaps (short):** **PDF/OMR (1.9m)** **resolved** via client rasterization + self-hosted Docker path (2026-04-19). **symbolic intake (1.9n)** **shipped** (2026-04-07) — mislabeled `.txt`/`.mxml`/MIDI sniff, Playground engine preview for non-`.xml`, Document ZIP-as-`.xml`, `musicXmlMarkers`; **MIDI** loads via `createRequire(import.meta.url)`; **RiffScore piano 404s mitigated** (Salamander CDN via `patch-package`); **Sandbox exports** **shipped** (2026-04-13) — live flush, MusicXML/MIDI/PNG/WAV/ZIP/JSON/chord-chart/print from tactile score; see [progress.md — Tactile Sandbox exports](progress.md#wl-sandbox-exports-2026-04-13); **`riffscoreAdapter` / `toolbarPlugins`:** **mitigated (2026-04-18)** with a narrow `ui` cast so **`npm run build`** passes (module augmentation optional long-term); **`make lint-frontend`** exits **0** with a few **`react-hooks/exhaustive-deps` warnings** — use **`make verify-strict`** for full gate; **Theory Inspector** live evidence refresh on send **shipped** (2026-04-06); residual **idea-action** and **model** edge cases possible.
- **Program state (2026-04-27):** Sandbox **flush-aware transpose** + **unified toolbar/palette dispatch** (**`handleToolSelect`**) + Document Advanced hidden — [progress.md — Last updated 2026-04-27](progress.md#last-updated-2026-04-27), [work log](progress.md#wl-sandbox-reliability-2026-04-27). **Same day:** **keyboard arrows** = **naturals-only diatonic** path vs **toolbar chromatic** — [naturals / LLM / audit work log](progress.md#wl-sandbox-naturals-llm-audit-2026-04-27). **Watch:** toolbar **`Octave ↓`** — re-verify after **`onToolbarAction`** path; historical intermittent failures tracked in **[Iteration 7 follow-up](progress.md#wl-iteration-7-followup-2026-04-25-pm)**.
- **Program state (2026-04-24):** Product voice + Playground invite — [progress.md — Cross-surface UX 2026-04-24](progress.md#wl-ux-team-playground-2026-04-24). **2026-04-23:** Credits — [progress.md — Team page 2026-04-23](progress.md#wl-team-credits-2026-04-23) (`TeamNavButton`, `/team`); sandbox fluidity — [progress.md — Sandbox fluidity 2026-04-23](progress.md#wl-sandbox-fluidity-2026-04-23); sandbox/export/clef — [progress.md — Sandbox & export polish 2026-04-23](progress.md#wl-sandbox-ux-polish-2026-04-23); **2026-04-22** MVP + Inspector — [progress.md — Program narrative 2026-04-22](progress.md#program-narrative-2026-04-22); **rest hover** — [progress.md — Sandbox score UX](progress.md#wl-sandbox-score-ux-2026-04-22); older table — [2026-04-20 narrative](progress.md#program-narrative-2026-04-20).
- **Generate timeout mitigation (2026-04-07):** SATB solver **`auto`** mode now tries greedy first for all slot counts, file routes default solver wall-clock to ~108s when `HF_SOLVER_MAX_MS` is unset, and Document timeout defaults to 180s with clearer copy. See [progress.md — Generate timeout mitigation](progress.md#wl-generate-timeout-2026-04-07).
- **Docs & deploy:** README set + **[docs/README.md](README.md)** index + **[deployment.md](deployment.md)** (Vercel vs Docker). Root **`.gitignore`** ignores accidental **`/node_modules/`** — see [progress.md](progress.md) work log.

### Current snapshot (detail)

- **First-run UX:** With **`COACHMARKS_ENABLED`**, the **coachmark tour** auto-starts on **`/`** (modal/coachmark cards disabled); otherwise **`OnboardingModal`** until dismissed. **`getServerOpenAIEnv()`** / **`llmClient`** apply **`OPENAI_API_KEY`** and optional **`OPENAI_BASE_URL`** / **`OPENAI_URL`** (see [deployment.md](deployment.md), [progress.md — LLM env](progress.md#wl-llm-env-2026-04-20)).
- **Preview:** Raw `.xml`/`.musicxml` in-browser (plus **ZIP-as-`.xml`** on Document: sniff → same preview API). **Everything else** (`.pdf`, `.mxl`, `.mid`, `.mxml`, `.txt`, extensionless, etc.) uses **`POST /api/to-preview-musicxml`** on Playground so the engine sniffs ZIP/MIDI/MusicXML; Zustand **`previewMusicXML`** when intake succeeds. **PDF on Document (2026-04-20):** after `useClientPdfPreview` rasterizes pages, the client also **`POST`s `pages[]` PNGs** to **`/api/to-preview-musicxml`** so a melody MusicXML can appear in the preview panel when OMR is available — not only a raster thumbnail. See [progress.md — Symbolic intake](progress.md#wl-intake-symbolic-2026-04-07) and [Palettes / PDF verification](progress.md#wl-palettes-repitch-2026-04-20).
- **Generation runtime behavior:** `POST /api/generate-from-file` now prefers a greedy-first SATB fast path in auto mode, with bounded default file-route solver wall-clock (`HF_SOLVER_MAX_MS` unset → ~108s; `0` disables).
- **Inspector UX:** Deterministic panels (**What this click means**, **Verifiable score export**); **`react-markdown`** on bubbles; **InspectorScoreFocus** + **`regionExplainContext`** for measure/part; dark-theme tokens on chat and playback bar; **split column** (note details + ideas vs chat); **IDEA_ACTIONS** rows with Accept/Reject; optional **inspector debug strip** when apply fails.
- **Tests (frontend):** Vitest covers `noteExplainContext`, `noteInsightAiSplit`, **`ideaActionResolve`**, **`staffPreviewPitch`**, `regionExplainContext`, `playbackScrub`, `studyConfig`, **`needsEnginePreviewForExtension`**, **`isProbablyZipBytes`**, **`musicXmlMarkers`**, **`intakeErrorHints`**, **`scoreToMidi`**, **`restsToNotes` / repitch** (`scoreUtils.test.ts`), **`rasterizePdf`** (`useClientPdfPreview.test.ts`), etc.
- **Exports (2026-04-13, refined 2026-04-20):** Modal formats read **flushed** Zustand score; MIDI/PNG/WAV/ZIP client-side. **PNG** captures the Export modal preview with **`RiffScoreEditor presentation`** (no toolbar). **PDF / Print** use **`ExportPrintRoot`** + **`body.hf-printing-score`** so only the score prints — not palette, inspector, or bars strip. Legacy **`hf-print-hide`** rules remain for older paths. Tablature still deferred.
- **Playback scrub:** Shipped — verify toolbar **Play**, **Space**, and **P** after scrub; see [progress.md](progress.md) for regression notes.

**Milestones:** M3 complete. M4 consolidated in [#79](https://github.com/salt-family/harmonyforge/issues/79) (MVP scope). **M5:** instrumentation in-repo; protocol/surveys are researcher-owned.

## M5 — User study (RQ1 / RQ2) — app instrumentation

**RQ1 — Ownership (reviewer vs generator):**

- **`generator_primary`** (default): Document → `POST /api/generate-from-file` → Sandbox with additive harmonies (existing MVP).
- **`reviewer_primary`:** Same upload/preview/config UX; primary CTA loads **melody-only** MusicXML into Sandbox (no harmony generation). Participants add harmonies manually; Theory Inspector (audit, tutor, stylist) unchanged.

**Activation:** `NEXT_PUBLIC_HF_STUDY_CONDITION` or first-load query `?study=reviewer_primary` (persisted in `sessionStorage` via `StudySessionProvider`). Framing copy differs on Playground, Document, and Sandbox onboarding when `reviewer_primary`.

**RQ2 — Trust in explanations (stylist suggestions):**

- **`full`** (default): Stylist structured response includes `rationale` + `summary`; panel shows full suggestion copy.
- **`minimal`:** `suggestionExplanationMode` on `POST /api/theory-inspector/suggest` strips prose; `SuggestionCard` shows neutral one-line header + pitch rows + `ruleLabel` only.

**Activation:** `NEXT_PUBLIC_HF_SUGGESTION_EXPLANATION_MODE` or `?hfExplain=minimal` (sessionStorage). Client sends `suggestionExplanationMode` with each suggest request.

**Consent & logging:** Optional gate when `NEXT_PUBLIC_HF_STUDY_REQUIRES_CONSENT=true`. Opt-in interaction log (`studyEventLog.ts`): generate vs skip-generation, note click, audit, explain/suggest actions, suggestion accept/reject; **Copy JSON** on Sandbox (`StudyLogExportBar`). No score payload in events.

**Code:** `frontend/src/lib/study/studyConfig.ts`, `studyEventLog.ts`, `readMelodyXml.ts`, `components/study/*`, `vitest` `studyConfig.test.ts`. Env template: `frontend/.env.example` (`OPENAI_API_KEY`, optional `OPENAI_BASE_URL` for Inspector).

## Objective

Build **HarmonyForge** — a Glass Box co-creative system that bridges the "Repair Phase Bottleneck" in symbolic music arrangement. The system rejects probabilistic "Black Box" AI in favor of deterministic, rule-based logic and explainable artificial intelligence (XAI). Goals:

- **Expressive Sovereignty**: Provide a "Jumping Pad" that respects the user's train of thought; the musician remains the ultimate author.
- **Copyright Safety by Design**: Use axiomatic music theory (e.g., Schenkerian analysis) instead of pattern mimicry from copyrighted datasets.
- **Pedagogical Partner**: Elevate AI from a flawed oracle into a transparent, explainable Theory Tutor.

## Research

**Repository layout (2026-04+):** Single Next.js app in **`frontend/`** (engine at **`frontend/src/server/engine/`**, route handlers under **`frontend/src/app/api/`**). **`docs/`** (plan, progress, ADRs, **`Taxonomy.md`**, context, **`design/`**). **`miscellaneous/`** (vendored **`pdfalto/`**). Root: **`Makefile`**, **`Dockerfile`**, **`requirements.txt`**, **`README.md`**, **`.gitignore`**, **`.cursor/`**. Older docs may say `backend/engine/` — that code now lives under **`frontend/src/server/engine/`**.

From HF LitReview notebook: 57 sources on AI-driven symbolic music generation. Key findings:

**Frontend baseline**: `frontend/` (Next.js, React, **RiffScore** as the primary editor, Tone.js for playback, Zustand for score and UI state) provides the Tactile Sandbox. VexFlow and OSMD remain for historical tooling and some fallback/display paths; the **canonical editing path** is RiffScore + `EditableScore` sync.

- Modern frameworks prioritize **musical structure and coherence** via subtask decomposition (high-level planning vs. note-level creation).
- "Glass Box" explainability and **deterministic logic** build user trust by aligning outputs with established music theory.
- The "Repair Phase" bottleneck is addressed by tactile workspaces for manual editing, ensuring **creative agency**.
- Neuro-symbolic integration and hierarchical modeling enable AI to capture nuanced thematic development.

## Plan

Build order (from MVP scope):

1. **Logic Core (Backend)** — Deterministic constraint-satisfaction solver
   - [x] **1.1** Scaffold backend: `engine/` package (Node.js/TypeScript), `package.json` with `test`, `lint`, `dev:backend`; wire Makefile
   - [x] **1.2** Define shared types: `LeadSheet`, `ChordSlot`, `SATBVoices`, pitch/range helpers; JSON schema for input/output
   - [x] **1.3** Implement chord parser: Roman numerals (I, ii, V7, etc.) → pitch classes; key context (C major, A minor)
   - [x] **1.4** Implement backtracking solver: assign S/A/T/B per chord slot; enforce range, spacing, parallel fifths/octaves, voice crossing
   - [x] **1.5** Melody placement: soprano = given melody when provided; else solver chooses within range
   - [x] **1.6** REST API: `POST /api/generate-satb` accepts JSON lead sheet, returns SATB JSON; Express or Hono on port 8000
   - [x] **1.7** Unit tests: chord parser, constraint checks, solver; integration test for API
   - [x] **1.8** File format pipeline: accept XML, PDF, MIDI; convert to canonical format; extract melody; generate harmonies; output MusicXML
   - [x] **1.8a** Input parsers: MusicXML (partwise + timewise via fast-xml-parser; no DTD loading; namespaces, grace notes, chords), MIDI (@tonejs/midi), MXL (adm-zip + ZIP sniff). PDF: pdfalto + Poppler + oemer (`fileIntake.ts`).
   - [x] **1.8b** Canonical format: internal `ParsedScore` (melody notes, key, chords if present). XML/MIDI → ParsedScore.
   - [x] **1.8c** Chord inference: if no chords in file, infer simple diatonic progression from melody (e.g. I–IV–V–I per phrase).
   - [x] **1.8d** Output: SATB solver result → MusicXML (SATB grand staff) via custom builder.
   - [x] **1.8e** New endpoint: `POST /api/generate-from-file` (multipart: file) → returns MusicXML string for Tactile Sandbox.
   - [x] **1.9** Engine config support: Extend `generate-from-file` to accept optional JSON config (instruments, mood). Use config in chord inference and MusicXML output.
   - [x] **1.9a** Types: Add `GenerationConfig` { instruments?: Record<Voice, string[]>, mood?: "major"|"minor" }.
   - [x] **1.9b** API: Accept `config` form field (JSON string). Validate; pass to pipeline.
   - [x] **1.9c** Chord inference: mood → key mode when inferring chords (major vs minor progressions).
   - [x] **1.9d** MusicXML: Use `instruments` for part names in output (one label per voice).
   - [x] **1.9e** Additive harmonies: melody stays as Part 1; selected instruments added as harmony parts. Partwise output for MuseScore/OSMD.
   - [x] **1.9f** CLI test: `make test-engine`, `scripts/run-engine-cli.ts`, input/output folders.
   - [x] **1.9g** Unique parts per instrument: When multiple instruments share a voice range (e.g., flute + violin both soprano), each gets a distinct harmony part via different chord tones + octave displacement (HFLitReview: divisi, register transfer).
   - [x] **1.9h** Robust MusicXML input: score-partwise and score-timewise; namespaced XML (MuseScore 4.x); grace notes skipped; chords (first pitch); no DTD loading.
   - [x] **1.9i** Robust MIDI parsing: first track with notes (Format 1); extract key + time sig from header.
   - [x] **1.9j** PDF input: `pdfalto` (ALTO + embedded MusicXML text fast path) → Poppler `pdftoppm` (first page) → `oemer` OMR → MusicXML; env `PDFALTO_BIN`, `POPPLER_PDFTOPPM`, `OEMER_BIN`. No Audiveris. Dropzone accepts `.pdf`.
   - [x] **1.9k** MXL support: parse compressed MusicXML (adm-zip); ZIP sniff for mislabeled `.xml`; MuseScore/MXL uploads.
   - [x] **1.9l** Document preview parity for non-XML files: `POST /api/to-preview-musicxml` + `parsedScoreToPartwiseMelodyMusicXML` (`engine/satbToMusicXML.ts`); Playground fetches before `/document` for `.pdf`/`.mxl`/`.mid`/`.midi`; `useUploadStore.previewMusicXML`; Document uses store + same `parseMusicXML` as FileReader path. Playground errors in-page; engine surfaces **oemer** stderr excerpt on failure; darwin/Homebrew **PATH** in `dev:backend`; pdfalto discovery via `argv[1]` walk + `cwd`.
   - [x] **1.9n** **Symbolic intake hardening (2026-04-07):** Engine `fileIntake` — MIDI magic sniff, `looksLikeMusicXml` + bounded UTF-8 peek, `.mxml`, mislabeled `.txt`/extensionless, prefixed embedded MusicXML roots, empty XML guard; `musicXmlMarkers.ts`; `midiParser` loads `@tonejs/midi` via `createRequire(import.meta.url)`. Frontend — `needsEnginePreviewForExtension` (engine preview for all extensions except `.xml`/`.musicxml`), Document ZIP sniff for `.xml`, `readMelodyXml` prefers `storePreviewXml`, shared client markers + fail-fast parse. Does **not** replace **1.9m** for PDF.
   - [x] **1.9m** **Shipped (2026-04-19):** Production path for PDF→MusicXML is now two-layered:
     - **Client-side rasterization** with `pdfjs-dist` in `frontend/src/hooks/useClientPdfPreview.ts` — every PDF gets a visible preview page on `/document`, even on Vercel.
     - **Server OMR** via the self-hosted Docker image (`Dockerfile` at repo root) that bundles pdfalto + Poppler + Python/oemer; `preflight-omr.sh` caches ONNX checkpoints to a Docker volume so first-use latency stays small.
     - **Multi-page support** — `intakeImagePagesToParsedScore` + `mergeParsedScores` in `frontend/src/server/engine/parsers/fileIntake.ts` accept N PNGs from the client and concatenate the parsed parts. Closes the old "page 1 only" footnote.
     - **Vercel path** — when no OMR binaries are present the engine returns a clear 501 with actionable copy, surfaced via `intakeErrorHints.ts`.
   - [x] **1.9o (2026-04-22):** Optional **`pickupBeats`** (0–3) in **`config`** for **`generate-from-file`** — **`parseConfig`** in `runtime.ts`, merge into parsed score before **`ensureChords`**; tests in **`filePipeline.test.ts`**.
   - [x] **1.9p (2026-04-23):** **`bassRhythmMode`** (`follow` \| `pedal`) — bass holds chord-slot spans while inner voices keep **`rhythmDensity`**; **`satbToMusicXML`**, **`parseConfig`**, Document + Zustand; tests in **`filePipeline.test.ts`**.

2. **Tactile Sandbox (Frontend)** — Design around existing `frontend/`
   - **Pages (design baseline)**: `/` Playground (upload) → `/document` Config (mood, instruments) → `/sandbox` Edit (**RiffScore** editor, Theory Inspector). See [progress.md](progress.md) Phase 4 for full page table.
   - [x] Fix Next.js dev server (was missing `node_modules`; run `npm install`)
   - [x] Frontend already has: **RiffScore** (primary sandbox editor), TheoryInspectorPanel, ScoreDropzone, EnsembleBuilderPanel; legacy VexFlow / OSMD / ScoreCanvas pieces remain where historical code paths reference them
   - [x] **2a** Document page config: Add mood (major/minor) selector to EnsembleBuilderPanel. Lift instrument selections + mood; pass to backend on Generate.
   - [x] **2a-classical (2026-04-20):** Document **no longer** exposes **Genre** in the main flow; **disclaimer** states **basic classical-style** harmony; default **`config`** uses **`genre: "classical"`**. **(2026-04-22)** **Advanced** optionally sets **`preferInferredChords`** + **`pickupBeats`** when the user opts in — [MVP work log](progress.md#wl-mvp-phase-ship-2026-04-22). **(2026-04-27)** **Advanced** controls (**pickup**, **prefer inferred chords**) **hidden** from **`EnsembleBuilderPanel`** UI — generation still reads **`useGenerationConfigStore`** — [2026-04-27 work log](progress.md#wl-sandbox-reliability-2026-04-27).
   - [x] **2a-ensemble-layout (2026-04-21):** **`EnsembleBuilderPanel`** — collapsible Glass Box pedagogy + **always-visible** algorithms-vs-AI + Theory Inspector disclosure + **Sound & style** / **Instruments (SATB)** cards; see [progress.md — Ensemble Builder UI](progress.md#wl-ensemble-builder-ui-2026-04-21).
   - [x] **2b** Wire backend API: `POST /api/generate-from-file` with multipart file + JSON config (instruments, mood); store MusicXML for sandbox.
   - [x] **2b-bar-regenerate-removed (2026-04-20):** Sandbox **“Regenerate harmony in bar”** control (localized re-generate + merge) **removed**; full score generation remains on **Document** only. Same [work log](progress.md#wl-learner-notes-scope-2026-04-20).
   - [x] **2c** Enable direct note manipulation: MusicXML render (VexFlowScore), selection, edit tools (Undo/Redo/Cut/Copy/Paste/Delete), duration/pitch/articulation/dynamics/measure/score tools. See Tactile Sandbox Note Editor plan.
   - [x] **2d** Document page preview: Parse uploaded MusicXML client-side; render in ScorePreviewPanel left pane; support score-partwise and namespaced MusicXML. **Extension:** PDF/MXL/MIDI use server-built preview XML from store when intake succeeds (**1.9l**); **PDF OMR still unreliable** (**1.9m**).
   - [x] **2e** VexFlow multi-measure SATB: Resolved via 2g.1 (fixed 4 beats per measure; pad per measure).
   - [x] **2f** Variable-part generation: Output only parts with selected instruments (violin+cello → 2 parts; 5 instruments → 5 parts). Relaxed solver fallback when strict fails.
   - [x] **2g** Note Editor (Noteflight/MuseScore-style) — Evolve the note editor in `sandbox/page.tsx` toward industry-standard UX. See references: [Noteflight Editor UI](https://robbyshaver.com/noteflight/editorUI2/pages/), [MuseScore Note Input](https://musescore.org/en/handbook/3/note-input), HFLitReview (tactile sandbox, repair phase, voice lanes).
     - **Vision**: Direct manipulation, minimal mode switching; duration palette + pitch input (keyboard A–G, mouse click on staff, optional MIDI/virtual piano). Edit-Authority for Expressive Sovereignty.
     - [x] **2g.1** Resolve VexFlow TickMismatch: fixed 4 beats per measure, pad per measure, floating-point rounding. **Display:** Prefer OSMD when musicXML exists (blank canvas fix); VexFlow when no musicXML or on crash fallback.
     - [x] **2g.2** Click-on-staff note placement: duration tool active → click canvas inserts note; selection-based or first-measure target.
     - [x] **2g.3** Keyboard shortcuts: A–G for pitch (**setPitchByLetter**), 1–6 for duration; **with selection**, **↑/↓** = **diatonic white-key step** (**natural** stored pitches — **`naturalDiatonicStepNotes`**); **⌘/Ctrl+↑/↓** = octave + natural spelling; **toolbar / palette +2/−2** = chromatic whole step (**`transposeNotes`**). **(2026-04-27)** Cursor / note-input **↑/↓** without transpose targets still moves staff per **`sandboxScoreKeyboard`**.
     - [x] **2g.4** **Sandbox chrome (2026-04-23):** The old **`SandboxActionBar`** strip (View/Edit, Undo, Redo, Delete) was **removed** — redundant with **hotkeys**, **RiffScore** toolbar, and **palette**; sandbox **`notationMode`** stays **`edit`**. Historical intent: Noteflight-style always-visible actions; superseded for lower clutter. See [progress.md — Sandbox fluidity (2026-04-23)](progress.md#wl-sandbox-fluidity-2026-04-23).
     - [x] **2g.5** Voice lanes: activePartId tracks selected part for insertion; part isolation for bounded edits.
   - [x] **2g.6** Rest-complete measures + rest-preserving round-trip: score normalization now fills incomplete measures with rests and trims trailing filler rests on overfill; RiffScore adapter preserves rest events both directions.
   - [x] **2g.7** Modern editing affordances: compact quick-toolbar in `/sandbox` (duration palette, rest insertion, tie toggle, accidental buttons, transport controls) plus expanded keyboard set (Cmd/Ctrl+C/X/V/A, Cmd/Ctrl+Y redo, `N` note-input arm, `0` rest insertion/convert selection, `?` shortcut help).
   - [x] **2g.8** RiffScore as primary sandbox editor: `patch-package` patch adds `ui.toolbarPlugins` to native toolbar; Zustand sync (`useRiffScoreSync`); stable store selectors to avoid React snapshot loops; `.env.example` / `.env.local` for optional `OPENAI_API_KEY`.
   - [x] **2g.9** **Rest → note re-pitch (2026-04-20; hover ghost 2026-04-22):** MuseScore-style "type over rest" + Noteflight "A–G on selected rest" — selecting a rest (or typing into a rest slot at the insertion cursor) swaps it for a note of the **same duration** at a neighbor-derived octave. New helpers `restsToNotes` / `convertRestToPitch` in [`scoreUtils.ts`](../frontend/src/lib/music/scoreUtils.ts); `setPitchByLetter` + `transposeNotes` no longer skip rests; keyboard path in [`sandbox/page.tsx`](../frontend/src/app/sandbox/page.tsx) short-circuits when the cursor is on a rest; [`RiffScoreEditor.tsx`](../frontend/src/components/score/RiffScoreEditor.tsx) shows a hint near the selected rest (**hover + click** ghost or type **A–G**). **Insertion / paste:** `resolveInsertionTarget` and `pasteNotes` use the **rest index** as **replace**, not insert-before (`insertNote` rest branch). **Hover ghost:** `pickRestHitAt` + `pitchAtStaffVerticalInContainer` on `pointermove`; `handleRestInputCommit` passes the slot’s **`duration` / `dots`** into `insertNote`. Ghost **off** when `noteInspectionEnabled`. Vitest: [`scoreUtils.test.ts`](../frontend/src/lib/music/scoreUtils.test.ts), [`riffscorePositions.test.ts`](../frontend/src/lib/music/riffscorePositions.test.ts). **Residual:** touch / dense-score hit-test / pitch drift — see [progress.md — Sandbox score UX](progress.md#wl-sandbox-score-ux-2026-04-22).
   - [x] **2g.10** **Palette panel (2026-04-20):** MuseScore/Noteflight-parity palette panel docked right of the score. [`SandboxPalettePanel.tsx`](../frontend/src/components/organisms/SandboxPalettePanel.tsx) + [`PaletteButton.tsx`](../frontend/src/components/atoms/PaletteButton.tsx) render collapsible sections driven by [`paletteRegistry.ts`](../frontend/src/lib/palettes/paletteRegistry.ts): Clefs, Key Signatures, Time Signatures, Barlines, Accidentals, Articulations & Ornaments, Dynamics, Lines, Repeats & Jumps, Tempo, Text, Tuplets, Breaths. Shortcut **F9** toggles visibility. New reducers (`setMeasureBarline`, `setMeasureRepeatMark`, `setMeasureTempoText`, `setOrnament`, `setTuplet`, `setLineOnSelection`, `setNoteLyric`, `setNoteChordSymbol`) extend [`scoreUtils.ts`](../frontend/src/lib/music/scoreUtils.ts); [`scoreTypes.ts`](../frontend/src/lib/music/scoreTypes.ts) gains `BarlineStyle`, `Measure.barline / repeatMark / rehearsalMark / tempoText`, plus `Note.ornament / tuplet / lyric / chordSymbol / lineStart / lineEnd`. Palette clicks re-use the existing `handleToolSelect` dispatcher so keyboard + toolbar + palette share one undo history.
   - [x] **2g.11** **Learner pitch labels (2026-04-20):** Optional **letter + accidental** (e.g. C, F#, Bb) **above** noteheads — `useScoreDisplayStore`, toggles in Sandbox header + Document preview; **`learnerPitchLabel`**, **`riffscorePositions`** (testid + rest-aware walks, **`getRsToHf()`**), learner overlay **z-order** + **toolbar clip** + FAB/palette **z-50**; export/print paths stay clean. See [progress.md — Learner note names & scope…](progress.md#wl-learner-notes-scope-2026-04-20) and [progress.md — Label refinement…](progress.md#wl-learner-pitch-labels-refine-2026-04-20).
   - [x] **2g.12** **Iteration 7 product polish (2026-04-25)** + **Sandbox reliability + naturals keyboard (2026-04-27):** Core themes shipped from **[Iteration7.txt](Iteration7.txt)** — unified keyboard pitch routing (`e.code`), drag/hit-test helper, Theory Inspector alternatives + rhythm guard, phrasing-vs-harmony copy, and melody-duration regression coverage in `filePipeline.test.ts`. **Follow-up (2026-04-25 PM):** removed timbre preview UI, Playground click burst polish, concise Harmony setup copy, Team footer new-tab behavior, Theory Inspector explanation empty-state prompt, preview scrollbar fixes, undo/redo reliability hardening. **2026-04-27 (initial):** **`getTransposeTargetNoteIds`**; Document **Advanced** hidden from **`EnsembleBuilderPanel`**. **2026-04-27 (flush parity + toolbar):** **`useScoreStore.getState().score`** after RS flush for **`transposeNotes`**; **`handleToolbarAction` → `handleToolSelect`** + **`return true`** on **`ScoreCanvas`** **`onToolbarAction`**. **2026-04-27 (naturals path):** **`sandboxScoreKeyboard`** uses **`scheduleNaturalDiatonicStep`** / **`scheduleTransposeNaturalLetters`** (not chromatic **`scheduleTransposeSelectedNotes`**); **`previewSandboxPitches`** → **Salamander** sampler; **`llmClient`** swap detection + **`configHint`**; concise **SATB audit** system message — **[progress.md — naturals / LLM / audit](progress.md#wl-sandbox-naturals-llm-audit-2026-04-27)**. **Watch:** toolbar **`Octave ↓`** — re-test in browser; **[Iteration 7 follow-up](progress.md#wl-iteration-7-followup-2026-04-25-pm)**.
   - [ ] JSON-based score deltas for state sync with backend (deferred to Theory Inspector Phase 3)

3. **Theory Inspector (Explainability)** — LLM-powered validation *(M4 #79 MVP wiring complete)*
   - [x] Inspector API route wired (`/api/theory-inspector`) with graceful fallback
   - [x] Auditor context wired via backend validation (`/api/validate-from-file`)
   - [x] Tutor chat wiring: Sandbox asks inspector API; replies render in panel
   - [x] Note-click explainability: clicking a generated harmony note triggers deterministic evidence + tutor LLM explanation (when API key is available)
   - [x] Original vs current pitch: generation baseline (harmony note id → pitch at load) + optional cached `validate-satb-trace`; inspector **Engine origin** vs **current pitch** sections; melody clicks = pitch-in-context only; tutor must not attribute user-edited pitch to the engine
   - [x] Engine trace source of truth: `/api/validate-satb-trace` provides per-slot rule findings consumed by inspector highlights + note explainability
   - [x] **Dual-mode Theory Inspector:** Origin Justifier vs Harmonic Guide (`inspectorMode`); `Note.originalGeneratedPitch` stamped on load + preserved across RiffScore sync; tutor API `theoryInspectorNoteMode`; SATB/additive FACT lines include next chord moment and barline neighbors; clipboard paste drops provenance
   - [x] **Multi-part note context:** Staff roster (input vs generated names); cross-part interval FACTs to every other staff at the beat; SATB note-explain only when exactly four parts (`requireExactlyFourParts`); SATB FACTs use instrument/part names; scores with 5+ staves use full additive context (`npm run test` in `frontend` for `noteExplainContext`)
   - [x] Stylist structured suggestion flow (`/api/theory-inspector/suggest`) with accept/reject + batch actions
   - [x] Grammarly-style inline suggestion ghost notes (accept/reject in-score)
   - [x] Note-level audit highlighting pipeline (red = deterministic violations, blue = theory nuance/warnings)
   - [x] Violation-card actions wired ("Explain more", "Suggest fix", "Show in score")
   - [x] **Source-aligned taxonomy + prompts (HF LitReview / NotebookLM):** `Taxonomy.md` source spine and engine-mapping table (Fux → solver motion **proxy**; Aldwell & Schachter → `constraints.ts` / `validate-satb-trace`; Caplin vocabulary + **honesty** for primary `engine/` path; Open Music Theory as **primary RAG pedagogy**); mirrored in `frontend/src/lib/ai/taxonomyIndex.ts`; auditor/tutor/stylist rules in `prompts.ts` (implementation vs heuristic; Caplin guardrails); `engine/solver.ts` comment; legacy chamber demo `planStructuralHierarchy` disclaimer (historical; not shipped)
   - [x] **Tutor output policy:** `prompts.ts` — `CITATION_AND_BREVITY` + persona tuning so LLM answers cite **one** appropriate source when stating theory norms, stay **down-to-earth and short** by default, and avoid citation overload (offline fallback unchanged).
   - [x] **Honest tutor tone:** `prompts.ts` — `HONESTY_NO_SYCOPHANCY` for Auditor/Tutor/Stylist (no flattery; distinguish constraint pass vs musical quality; admit incomplete context; gray areas; correct wrong premises; Stylist tradeoffs / “one common fix”).
   - [x] **Note-inspector UX (2026-04):** `TheoryInspectorPanel` — plain-language titles + subtitles; **`aiSuggestions`** from tutor stream (`<<<SUGGESTIONS>>>`); `noteInsightAiSplit.ts` + vitest; `useTheoryInspectorStore.aiSuggestions`. **2026-04 follow-up:** panel order **What the tool first wrote → What this click means → Verifiable export → Tutor summary → Ideas to try next** (ideas after summary); deterministic **why** for axiomatic engine in **What this click means** (`buildAxiomaticEngineWhyParagraph`, additive path); tutor prompts require **what + why** on each suggestion bullet (`NOTE_EXPLAIN_TUTOR_BRIEF`, `prompts.ts` tutor).
   - [x] **Staff ↔ instrument on canvas:** `extractStaffLabelLayout` (`riffscorePositions.ts`); `RiffScoreEditor` left labels + **Staves (top → bottom)** fallback when DOM doesn’t match part count.
   - [x] **Inspector scroll + React Compiler:** `useLayoutEffect` without dependency array (avoids “useEffect final argument changed size” under Next 16 + Turbopack); see `@progress.md`.
   - [x] **Free-form chat in panel (2026-04-03):** `TheoryInspectorPanel` shows Zustand **`messages`** + always-visible composer (empty placeholder); sandbox wires **`sendMessage`** / **`inputValue`** / **`streamingMessageId`**; no automatic **`chips`** rows after assistant replies or after **`runAudit`**; conversation history built before the in-flight empty AI bubble. See `@progress.md` → **Work log — 2026-04-03**.
   - [x] **Editor focus for chat + measure/part UI (2026-04-04):** **`InspectorScoreFocus`** + **`scoreSelectionContext`**; **`Editor focus`** block in **`prompts.ts`** (Tutor/Auditor/Stylist) so the LLM reads clicked context; **`regionExplainContext.ts`** (`buildMeasureFocusFacts`, `buildPartFocusFacts`) + vitest; **`RiffScoreEditor`** measure strip, staff-label part select, selection inference, green **focus** overlays; **`TheoryInspectorPanel`** region card; route **`scoreSelectionContext`**. See `@progress.md` → **Work log — Theory Inspector: editor focus for chat + measure/part (2026-04-04)**.
   - [x] **Tutor text export + message ordering (2026-04-03):** **`SCORE_DIGEST`**, **`FACT: AUTHORITATIVE NOTATION`**, **`FULL BAR`** (measure-level `buildMeasureFocusFacts` appended to note evidence); **note-click** `userMessage` = evidence **then** **Response rules**; **chat** prepends full FACT block before user text; **SATB** path includes digest + full bar; **`prompts.ts`** / **`NOTE_EXPLAIN_TUTOR_BRIEF`** — full notation, no “pitch-only”; **UI** — **`ChatBubble`** AI variant + **`SandboxPlaybackBar`** pagination contrast for dark theme. See `@progress.md` → **Work log — LLM “sees” the score (2026-04-03)**.
   - [x] **Tutor follow-up + panels + markdown (2026-04-04):** **`sendMessage`** — snapshot **`conversationHistory`** before **`addMessage(userMsg)`** (no duplicate plain-then-rich user pair to the LLM). **Panels** — **What this click means** + **Verifiable score export**; shortened **`currentPitchGuideExplanation`**. **`react-markdown`** + **`MarkdownText.tsx`** — **`ChatBubble`** user/ai; tutor summary, ideas, origin, click-meaning in **`TheoryInspectorPanel`**. See `@progress.md` → **Work log — Tutor follow-up + panels + markdown (2026-04-04)**.
   - [x] **Live evidence refresh on chat send (2026-04-06):** Before **`POST /api/theory-inspector`**, **`sendMessage`** rebuilds **`scoreSelectionContext`** from the flushed live score (measure/part via **`regionExplainContext`**; note via **`buildLiveNoteExplainInsight`**); updates focus / **`patchSelectedNoteInsight`** while preserving AI fields. See [progress.md — Holistic refinement program](progress.md#holistic-refinement-2026-04). **Residual:** model quality; **`make lint-frontend`** warning debt (not errors).
   - [x] **Fixed explanation depth (2026-04-03; reinforced 2026-04-22):** No **B / S / P** control in the panel; store keeps **`explanationLevel: "beginner"`** ( **`setExplanationLevel` removed** ); API **`resolveExplanationLevel`** still accepts body overrides for tests; prompts emphasize **beginner, thorough but concise**. See **[progress.md — 2026-04-22](progress.md#program-narrative-2026-04-22)**.
   - [x] **Inspector split layout + actionable ideas + ghost pitch labels (2026-04-06):** **`TheoryInspectorPanel`** — equal **top / bottom** panes (note details + ideas vs chat scroll); auto-scroll only the chat pane. **Ideas:** optional tutor block **`<<<IDEA_ACTIONS>>>`** JSON (`ideaActionSchema.ts`, **`splitNoteInsightAiContent`**); **`NoteInsight.ideaActions`** + **`patchSelectedNoteInsight`**; Accept applies via **`applySuggestion`**; Reject keeps row visible; study log **`idea_action_accepted` / `idea_action_rejected`**. **Ghosts:** stylist **`RiffScoreSuggestionOverlay`** always shows **`suggestedPitch`** label; note-input mode shows preview pitch via **`findNoteInputPreviewLayout`** + **`staffPreviewPitch.ts`**. **Apply reliability:** **`NOTE_IDS_FOR_IDEA_ACTIONS`** **`FACT:`** lines in additive + SATB tutor payload; **`resolveIdeaActionNoteId`** (`ideaActionResolve.ts`) when JSON `noteId` is wrong; sandbox **`setInspectorDebugStatus`** on failure. **Vitest:** **`ideaActionResolve.test.ts`**, **`staffPreviewPitch.test.ts`**, **`noteInsightAiSplit.test.ts`**. **2026-04-06 follow-up:** **`resolveIdeaActionNoteId`** — **longest substring-matched part name** (e.g. Violin II vs Violin); duplicate identical names still ambiguous. **Open:** off-beat suggestions, omitted **NOTE_IDS** — see [progress.md — Holistic refinement](progress.md#holistic-refinement-2026-04).

4. **Supporting Features** *(M4 #79 — MVP by 3/27)*
   - [x] **Team / credits page (2026-04-23; narrative + motion 2026-04-24):** Global **Meet the team** control (**`TeamNavButton`**) and **`/team`** route with creator profiles (bios, roles, quotes, **`next/image`**). **2026-04-24:** collapsible profile details, “how we work together” before product stack, shared tooltip/header polish, footer **Team** link — see **[progress.md — Cross-surface UX 2026-04-24](progress.md#wl-ux-team-playground-2026-04-24)** and **[Team page 2026-04-23](progress.md#wl-team-credits-2026-04-23)**. Static photos under **`frontend/public/creators/`**.
   - [x] **Playground first-run polish (2026-04-24):** Upload stand copy/layout (no **`nowrap`** clip), hover **pulse** + **musical-note** aureole — **`DropzoneCopy`**, **`UploadPromptContent`**, **`globals.css`** — same work log as above.
   - [ ] Multi-clef & instrument transposition; multi-instrument selection — **scoped in [ADR 003](adr/003-multi-clef-transposition-scope.md)** (vertical slice first; tab + JSON deltas explicitly deferred there)
   - [x] Audio playback: rest-aware timing + measure-aware scheduling; pitch-safe filtering; transport stability
   - [x] **RiffScore playhead scrub (2026-04):** `PlaybackScrubOverlay` + `playbackScrub.ts` (measure-span layout, **snap to nearest measure start**); `riffscorePlaybackBridge.ts` sets **`globalThis.__HF_RIFFSCORE_PLAY_FROM`** on release; **`patches/riffscore+1.0.0-alpha.9.patch`** wires **`consumeHfRiffScorePendingPlay()`** into **`handlePlayToggle`** and **`handlePlayback`** so **toolbar Play**, plain **Space**, and **P** start from scrubbed bar (quant `0`); **`Shift+Space`** / **`Shift+⌘|Alt+Space`** unchanged (replay / from start); native SVG cursor hidden. **Open:** manual regression pass; any new RiffScore play entry points must consume pending or they will ignore scrub.
   - [x] Onboarding flow: guided first-time tour (Upload → Document → Generate → Sandbox) with localStorage completion flag
   - [x] **Export (2026-04-13):** Live-score flush (`getLiveScoreAfterFlush`); MusicXML, JSON, MIDI (`scoreToMidi.ts`), PNG (export preview via `html-to-image`), WAV (`Tone.Offline` + `scoreToWav.ts`), ZIP (`fflate` + chord-chart API), chord-chart, print/PDF (`window.print()` + print CSS); coachmark opens export with same snapshot as header. **Open:** PNG = viewport only; MP3 not in stack; **`validate-from-file`** semantics unchanged. **Tablature deferred.**
   - [x] **Export cleanup — score only (2026-04-20):** ExportModal's `ScorePreviewPane` now renders `<RiffScoreEditor presentation />` (no toolbar, no bars strip, no FABs) so PNG captures only the score. `editableScoreToRiffConfig` accepts `showToolbar`. A dedicated hidden [`ExportPrintRoot.tsx`](../frontend/src/components/organisms/ExportPrintRoot.tsx) mounts on Sandbox; `printScoreOnly()` (in [`sandbox/page.tsx`](../frontend/src/app/sandbox/page.tsx)) tags `<body>` with `hf-printing-score` before `window.print()` and the new `@media print` rules in [`globals.css`](../frontend/src/app/globals.css) hide every other root. PDF/Print output no longer include palettes, bars strip, staff labels, or inspector chrome.

## Verification

- `make test` — Unified Vitest suite (**296** tests in `frontend/` as of **2026-04-27** when dependencies are installed); engine tests live at `frontend/src/server/engine/**/*.test.ts`
- `cd frontend && npm run test` — Same as `make test`; includes `noteExplainContext`, `noteInsightAiSplit`, `ideaActionResolve`, `staffPreviewPitch`, `regionExplainContext`, `playbackScrub`, `studyConfig`, `intakeErrorHints`, `needsEnginePreviewForExtension`, `isProbablyZipBytes`, `musicXmlMarkers`, `scoreToMidi`, `transpose`, `intentRouter`, `useGenerationConfigStore`, `parseConfig` / `filePipeline`, `measureRangeSlice`, and the full engine suite (`solver`, `chordInference`, `fileIntake`, `anacrusis`, etc.)
- **Sandbox exports (2026-04-13):** After editing in RiffScore, open Export → confirm XML/MIDI/JSON/WAV/ZIP match canvas; print preview hides chrome; **`riffscoreAdapter`** uses a narrow cast for **`toolbarPlugins`** (patch-package) — if upstream types change, re-check **`npm run build`**
- `make lint` — ESLint for the Next app (includes `src/server/engine`)
- `make lint-frontend` — Frontend ESLint (exit 0; may print **warnings** — see [progress.md](progress.md#holistic-refinement-2026-04))
- `make verify` — `test` + `lint` + `build`
- `make verify-strict` — `verify` + `lint-frontend` (stricter CI-minded gate)
- `make build` — Next production build (type-checks engine under `src/server/engine`)
- `make test-engine` — CLI runs [`frontend/scripts/run-engine-cli.ts`](../frontend/scripts/run-engine-cli.ts) with default input [`frontend/public/samples/tour_demo.xml`](../frontend/public/samples/tour_demo.xml) → `frontend/engine-cli-output/` (or pass `-i`/`-o`)
- Manual: Upload a partwise MusicXML (e.g. `frontend/public/samples/tour_demo.xml`) → preview on `/document` → configure mood + instruments → Generate Harmonies → land on `/sandbox` with the configured parts
- **Rest hover ghost (2026-04-22):** On `/sandbox`, **without** a duration tool selected, **hover** a rest — a **ghost notehead** and letter name should track **vertical** pointer position on that staff; **click** the ghost to replace the rest (**same written duration/dots**). With **Theory Inspector** note-inspection overlays on, the ghost should **not** appear (inspector hit targets take precedence). **Touch:** no hover — use **select rest + A–G** or keyboard paths; see [progress.md — Sandbox score UX](progress.md#wl-sandbox-score-ux-2026-04-22).
- **Playback scrub (2026-04):** On `/sandbox`, drag the vertical play line to a bar, release (snaps to measure start), then start with **toolbar Play**, **Space**, or **P** — audio should begin at that bar; **Shift+Space** (replay) should **not** use the scrub anchor. Re-run after `npm ci` to confirm **`patch-package`** applied.
- **Sandbox transpose (2026-04-27):** **Keyboard** with selection: **↑/↓** = **diatonic naturals** (white-key letter steps); **⌘/Ctrl+↑/↓** = **octave** with natural spelling — see [progress.md — naturals / LLM / audit](progress.md#wl-sandbox-naturals-llm-audit-2026-04-27). **Toolbar / F9 palette:** **+2 / −2 / 8+ / 8−** = chromatic **`transposeNotes`** — should match post-flush **`useScoreStore.getState().score`** + **`getTransposeTargetNoteIds`** — [progress.md — reliability 2026-04-27](progress.md#wl-sandbox-reliability-2026-04-27). **Re-verify:** toolbar **`Octave ↓`** ([Iteration 7 follow-up](progress.md#wl-iteration-7-followup-2026-04-25-pm)).
- **LLM deploy (2026-04-27):** **`OPENAI_API_KEY`** = **`sk-…`** only; **`OPENAI_MODEL`** = model id. If OpenAI **401** echoes **`gpt-4o-mini`**, vars were **swapped** — app **recovers** when **`sk-`** sits in **`OPENAI_MODEL`**; fix names in Vercel and **redeploy**. **`curl`** **`GET /api/theory-inspector`** may return **`configHint`**. See [deployment.md](deployment.md) and [progress.md](progress.md#wl-sandbox-naturals-llm-audit-2026-04-27).
- **RiffScore patch / editor errors (2026-04-23):** If the sandbox throws **`pendingClefChange is not defined`** or viola noteheads sit on the wrong lines, run **`cd frontend && rm -rf .next && npm install`** (postinstall runs **`patch-package`**). Confirm **`frontend/patches/riffscore+1.0.0-alpha.9.patch`** is present and **`patch-package`** prints **`riffscore@1.0.0-alpha.9 ✔`** during install.
- **File pipeline**: `POST /api/generate-from-file` returns `200` with partwise MusicXML (additive harmonies)
- **LLM (optional):** Copy `frontend/.env.example` → `.env.local`, set `OPENAI_API_KEY` (**secret**, not the model name) and `OPENAI_MODEL`, restart `make dev`; `curl http://localhost:3000/api/theory-inspector` should report `"hasApiKey":true`, optional `"configHint":null`, and when a custom gateway is configured `"hasCustomBaseUrl":true`
- **Theory Inspector follow-up (2026-04-04):** Sandbox → open inspector → click **melody** note → after tutor summary, ask e.g. “Is this a half note?” — expect answer grounded in **`SCORE_DIGEST` / AUTHORITATIVE** (not “I can’t see notation”); chat bubbles should render **`**bold**`** when the model emits markdown
- **Theory Inspector default depth:** With **`OPENAI_API_KEY`**, chat, suggest, and note tutor flows use fixed **beginner** audience instructions (no panel toggle; see **[progress.md 2026-04-22](progress.md#program-narrative-2026-04-22)**)
- **Known active limitation (2026-03-24):** VexFlow edit renderer still fails to produce visible notation for some generated scores; Sandbox now auto-falls back to safe OSMD preview in Edit mode to avoid blank-canvas regressions while preserving app usability.
- **PDF / OMR (2026-04-02):** Manual upload flow should call **`/api/to-preview-musicxml`** for non-XML formats; success requires **pdfalto** built, **Poppler**, and working **oemer** (see `requirements.txt`). **Failure is expected** until oemer checkpoints + Python stack are pinned — see **`@progress.md` → Multi-format intake & PDF → Document preview**.
- **M5 study prep:** Open `http://localhost:3000/?study=reviewer_primary` → upload MusicXML → Document shows **Continue to sandbox (melody only)** → Sandbox has melody-only score. Optional `?hfExplain=minimal` → stylist suggestions omit prose in panel (requires `OPENAI_API_KEY`). Opt-in **Research log** on Sandbox copies JSON when logging is enabled.
- **Production deploy:** Follow **[deployment.md](deployment.md)** — Vercel (**root = `frontend`**) or self-hosted **`Dockerfile`**; smoke-test upload → generate → sandbox on the live URL.
