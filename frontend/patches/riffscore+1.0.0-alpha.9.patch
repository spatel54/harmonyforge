diff --git a/node_modules/riffscore/dist/index.d.ts b/node_modules/riffscore/dist/index.d.ts
index 1ca537c..ee2d0eb 100644
--- a/node_modules/riffscore/dist/index.d.ts
+++ b/node_modules/riffscore/dist/index.d.ts
@@ -243,6 +243,19 @@ interface RiffScoreConfig {
         theme?: ThemeName;
         showBackground?: boolean;
         showScoreTitle?: boolean;
+        toolbarPlugins?: Array<{
+            id?: string;
+            label: string;
+            title?: string;
+            icon?: ReactNode;
+            onClick?: () => void;
+            showLabel?: boolean;
+            isActive?: boolean;
+            disabled?: boolean;
+            isEmphasized?: boolean;
+            isDashed?: boolean;
+            className?: string;
+        }>;
     };
     interaction: {
         isEnabled: boolean;
@@ -313,8 +326,21 @@ interface ScoreEditorContentProps {
     showScoreTitle?: boolean;
     enableKeyboard?: boolean;
     enablePlayback?: boolean;
+    toolbarPlugins?: Array<{
+        id?: string;
+        label: string;
+        title?: string;
+        icon?: ReactNode;
+        onClick?: () => void;
+        showLabel?: boolean;
+        isActive?: boolean;
+        disabled?: boolean;
+        isEmphasized?: boolean;
+        isDashed?: boolean;
+        className?: string;
+    }>;
 }
-declare const ScoreEditorContent: ({ scale, label, showToolbar, showBackground, showScoreTitle, enableKeyboard, enablePlayback, }: ScoreEditorContentProps) => react_jsx_runtime.JSX.Element;
+declare const ScoreEditorContent: ({ scale, label, showToolbar, showBackground, showScoreTitle, enableKeyboard, enablePlayback, toolbarPlugins, }: ScoreEditorContentProps) => react_jsx_runtime.JSX.Element;
 declare const ScoreEditor: ({ scale, label, initialData, }: {
     scale?: number;
     label?: string;
diff --git a/node_modules/riffscore/dist/index.js b/node_modules/riffscore/dist/index.js
index ef82e00..0ec3982 100644
--- a/node_modules/riffscore/dist/index.js
+++ b/node_modules/riffscore/dist/index.js
@@ -1165,12 +1165,25 @@ var CLEF_REFERENCE = {
 var getOffsetForPitch = (pitch, clef = "treble") => {
   if (!pitch) return 0;
   const normalizedPitch = getStaffPitch(pitch);
+  const ref = CLEF_REFERENCE[clef] || CLEF_REFERENCE.treble;
+  const match = normalizedPitch.match(/^([A-G])(\d+)$/);
+  if (clef === "alto" || clef === "tenor") {
+    if (!match) return 0;
+    const [, letter, octStr] = match;
+    const octave = parseInt(octStr, 10);
+    const refMatch = ref.pitch.match(/^([A-G])(\d+)$/);
+    if (!refMatch) return ref.offset;
+    const [, refLetter, refOctStr] = refMatch;
+    const refOctave = parseInt(refOctStr, 10);
+    const letterIdx = STAFF_LETTERS.indexOf(letter);
+    const refLetterIdx = STAFF_LETTERS.indexOf(refLetter);
+    const stepsFromRef = (octave - refOctave) * 7 + (letterIdx - refLetterIdx);
+    return ref.offset - stepsFromRef * 6;
+  }
   const mapping = getPitchToOffset(clef);
   if (mapping[normalizedPitch] !== void 0) {
     return mapping[normalizedPitch];
   }
-  const ref = CLEF_REFERENCE[clef] || CLEF_REFERENCE.treble;
-  const match = normalizedPitch.match(/^([A-G])(\d+)$/);
   if (!match) return 0;
   const [, letter, octStr] = match;
   const octave = parseInt(octStr, 10);
@@ -1184,6 +1197,12 @@ var getOffsetForPitch = (pitch, clef = "treble") => {
   return ref.offset - stepsFromRef * 6;
 };
 var getPitchForOffset = (offset, clef = "treble") => {
+  if (clef === "alto" || clef === "tenor") {
+    const ref = CLEF_REFERENCE[clef];
+    if (!ref) return "C4";
+    const stepsFromRef = Math.round((ref.offset - offset) / 6);
+    return movePitchVisual(ref.pitch, stepsFromRef);
+  }
   const mapping = getYToPitch(clef);
   return mapping[offset];
 };
@@ -3426,7 +3445,7 @@ var initTone = async (onState) => {
 var loadPianoSampler = (Tone) => {
   if (sampler) return;
   updateState({ instrumentState: "loading-samples" });
-  const baseUrl = "/audio/piano/";
+  const baseUrl = "https://tonejs.github.io/audio/salamander/";
   sampler = new Tone.Sampler({
     urls: {
       A0: "A0.mp3",
@@ -3492,9 +3511,24 @@ var scheduleTonePlayback = async (timeline, bpm, startTimeOffset = 0, onPosition
   }
   stopTonePlayback();
   Tone.Transport.bpm.value = bpm;
-  const filteredTimeline = timeline.filter((e) => e.time >= startTimeOffset);
+  const filteredTimeline = timeline.filter((e) => e.time >= startTimeOffset - 1e-6);
+  const totalEnd = timeline.reduce(
+    (max, e) => Math.max(max, e.time + (e.duration || 0)),
+    0
+  );
+  const playDuration = Math.max(0, totalEnd - startTimeOffset);
   if (filteredTimeline.length === 0) {
-    onComplete == null ? void 0 : onComplete();
+    if (playDuration > 0) {
+      Tone.Transport.scheduleOnce(() => {
+        updateState({ isPlaying: false });
+        clearHfPlaybackPositionIfInstalled();
+        onComplete == null ? void 0 : onComplete();
+      }, playDuration + 0.1);
+      Tone.Transport.start();
+      updateState({ isPlaying: true });
+    } else {
+      onComplete == null ? void 0 : onComplete();
+    }
     return;
   }
   const adjustedTimeline = filteredTimeline.map((e) => __spreadProps(__spreadValues({}, e), {
@@ -3502,12 +3536,20 @@ var scheduleTonePlayback = async (timeline, bpm, startTimeOffset = 0, onPosition
   }));
   const events = adjustedTimeline.map((e) => ({
     time: e.time,
-    note: e.pitch || freqToNote(e.frequency),
+    type: e.type || "note",
+    note: e.type === "rest" ? null : e.pitch || freqToNote(e.frequency),
     duration: e.duration,
     measureIndex: e.measureIndex,
     quant: e.quant
   }));
   const part = new Tone.Part((time, event) => {
+    notifyHfPlaybackPositionIfInstalled(event.measureIndex, event.quant, event.duration);
+    if (event.type === "rest" || !event.note) {
+      Tone.Draw.schedule(() => {
+        onPositionUpdate == null ? void 0 : onPositionUpdate(event.measureIndex, event.quant, event.duration);
+      }, time);
+      return;
+    }
     instrument.triggerAttackRelease(event.note, event.duration, time);
     Tone.Draw.schedule(() => {
       onPositionUpdate == null ? void 0 : onPositionUpdate(event.measureIndex, event.quant, event.duration);
@@ -3516,9 +3558,10 @@ var scheduleTonePlayback = async (timeline, bpm, startTimeOffset = 0, onPosition
   currentPart = part;
   part.start(0);
   const lastEvent = events[events.length - 1];
-  const endTime = lastEvent.time + lastEvent.duration + 0.1;
+  const endTime = Math.max(lastEvent.time + lastEvent.duration, playDuration) + 0.1;
   Tone.Transport.scheduleOnce(() => {
     updateState({ isPlaying: false });
+    clearHfPlaybackPositionIfInstalled();
     onComplete == null ? void 0 : onComplete();
   }, endTime);
   Tone.Transport.start();
@@ -3547,8 +3590,48 @@ var scheduleChordPlayback = async (chordEvents, startTimeOffset = 0) => {
   chordPart = chordPartInstance;
   chordPartInstance.start(0);
 };
+var scheduleHfMetronomeIfInstalled = (Tone, bpm, startTimeOffset, totalEnd, timeSignature) => {
+  const g = typeof globalThis !== "undefined" ? globalThis : void 0;
+  const schedule = g && g.__HF_SCHEDULE_METRONOME;
+  if (typeof schedule === "function" && Tone) {
+    schedule(Tone, { bpm, startTimeOffset, totalEnd, timeSignature });
+  }
+};
+var clearHfMetronomeIfInstalled = (Tone) => {
+  const g = typeof globalThis !== "undefined" ? globalThis : void 0;
+  const clear = g && g.__HF_CLEAR_METRONOME;
+  if (typeof clear === "function" && Tone) {
+    clear(Tone);
+  }
+};
+var notifyHfPlaybackPositionIfInstalled = (measureIndex, quant, durationSec) => {
+  const g = typeof globalThis !== "undefined" ? globalThis : void 0;
+  const notify = g && g.__HF_ON_PLAYBACK_POSITION;
+  if (typeof notify === "function") {
+    notify(measureIndex, quant, durationSec);
+  }
+};
+var clearHfPlaybackPositionIfInstalled = () => {
+  const g = typeof globalThis !== "undefined" ? globalThis : void 0;
+  const clear = g && g.__HF_CLEAR_PLAYBACK_POSITION;
+  if (typeof clear === "function") {
+    clear();
+  }
+};
 var scheduleScorePlayback = async (timeline, score, bpm, chordConfig, startTimeOffset = 0, onPositionUpdate, onComplete) => {
+  const totalEnd = timeline.reduce(
+    (max, e) => Math.max(max, e.time + (e.duration || 0)),
+    0
+  );
   await scheduleTonePlayback(timeline, bpm, startTimeOffset, onPositionUpdate, onComplete);
+  const Tone = await loadTone();
+  scheduleHfMetronomeIfInstalled(
+    Tone,
+    bpm,
+    startTimeOffset,
+    totalEnd,
+    score == null ? void 0 : score.timeSignature
+  );
   if ((chordConfig == null ? void 0 : chordConfig.enabled) && score.chordTrack && score.chordTrack.length > 0) {
     const chordEvents = createChordPlaybackEvents(score, bpm, chordConfig.velocity);
     await scheduleChordPlayback(chordEvents, startTimeOffset);
@@ -3557,6 +3640,8 @@ var scheduleScorePlayback = async (timeline, score, bpm, chordConfig, startTimeO
 var stopTonePlayback = () => {
   const Tone = getTone();
   if (!Tone) return;
+  clearHfMetronomeIfInstalled(Tone);
+  clearHfPlaybackPositionIfInstalled();
   Tone.Transport.stop();
   Tone.Transport.cancel();
   if (currentPart) {
@@ -3613,19 +3698,15 @@ var createTimeline = (score, bpm) => {
       let currentMeasureQuant = 0;
       measure.events.forEach((event, eIndex) => {
         const eventDurQuants = getNoteDuration(event.duration, event.dotted, event.tuplet);
+        const eventTime = measureStartTime + currentMeasureQuant * secondsPerQuant;
+        let hadPitch = false;
         event.notes.forEach((note) => {
           if (!note.pitch) {
-            if (process.env.NODE_ENV !== "production") {
-              console.warn("TimelineService: Note without pitch detected", {
-                measureIndex: mIndex,
-                eventIndex: eIndex,
-                note
-              });
-            }
             return;
           }
+          hadPitch = true;
           rawEvents.push({
-            time: measureStartTime + currentMeasureQuant * secondsPerQuant,
+            time: eventTime,
             duration: eventDurQuants * secondsPerQuant,
             pitch: note.pitch,
             tied: !!note.tied,
@@ -3635,10 +3716,25 @@ var createTimeline = (score, bpm) => {
             staffIndex
           });
         });
+        if (!hadPitch && eventDurQuants > 0) {
+          rawEvents.push({
+            time: eventTime,
+            duration: eventDurQuants * secondsPerQuant,
+            pitch: null,
+            isRest: true,
+            measureIndex: mIndex,
+            eventIndex: eIndex,
+            quant: currentMeasureQuant,
+            staffIndex
+          });
+        }
         currentMeasureQuant += eventDurQuants;
       });
     });
     rawEvents.sort((a, b) => {
+      if (a.isRest && b.isRest) return a.time - b.time;
+      if (a.isRest) return -1;
+      if (b.isRest) return 1;
       if (a.pitch !== b.pitch) return a.pitch.localeCompare(b.pitch);
       return a.time - b.time;
     });
@@ -3646,7 +3742,7 @@ var createTimeline = (score, bpm) => {
       let current = rawEvents[0];
       for (let i = 1; i < rawEvents.length; i++) {
         const next = rawEvents[i];
-        const isConnected = current.tied && next.pitch === current.pitch && Math.abs(next.time - (current.time + current.duration)) < 1e-3;
+        const isConnected = !current.isRest && !next.isRest && current.tied && next.pitch === current.pitch && Math.abs(next.time - (current.time + current.duration)) < 1e-3;
         if (isConnected) {
           current.duration += next.duration;
           current.tied = next.tied;
@@ -3659,6 +3755,18 @@ var createTimeline = (score, bpm) => {
     }
   });
   function addEventToTimeline(raw) {
+    if (raw.isRest) {
+      timeline.push({
+        time: raw.time,
+        duration: raw.duration,
+        type: "rest",
+        measureIndex: raw.measureIndex,
+        eventIndex: raw.eventIndex,
+        staffIndex: raw.staffIndex,
+        quant: raw.quant
+      });
+      return;
+    }
     const freq = getFrequency(raw.pitch);
     if (freq) {
       timeline.push({
@@ -3692,6 +3800,16 @@ var usePlayback = (score, bpm) => {
     });
     isInitialized2.current = true;
   }, []);
+  React2.useEffect(() => {
+    const ev = "riffscore-clear-playback-anchor";
+    const handler = () => {
+      setPlaybackPosition({ measureIndex: null, quant: null, duration: 0 });
+    };
+    if (typeof window !== "undefined") {
+      window.addEventListener(ev, handler);
+      return () => window.removeEventListener(ev, handler);
+    }
+  }, []);
   const exitPlaybackMode = React2.useCallback(() => {
     setIsActive(false);
   }, []);
@@ -3700,6 +3818,10 @@ var usePlayback = (score, bpm) => {
     setIsPlaying(false);
     setIsActive(true);
     setPlaybackPosition({ measureIndex: null, quant: null, duration: 0 });
+    lastPlayPosition = { measureIndex: 0, quant: 0 };
+    if (typeof globalThis !== "undefined") {
+      globalThis.__HF_RIFFSCORE_PLAY_FROM = null;
+    }
   }, []);
   const pausePlayback = React2.useCallback(() => {
     stopTonePlayback();
@@ -3708,24 +3830,43 @@ var usePlayback = (score, bpm) => {
   }, []);
   const playScore = React2.useCallback(
     async (startMeasureIndex = 0, startQuant = 0) => {
+      lastPlayPosition = { measureIndex: startMeasureIndex, quant: startQuant };
       await ensureInit();
       stopTonePlayback();
       setLastPlayStart({ measureIndex: startMeasureIndex, quant: startQuant });
       setIsPlaying(true);
       setIsActive(true);
       const timeline = createTimeline(score, bpm);
-      let startTimeOffset = 0;
-      const startEvent = timeline.find(
-        (e) => e.measureIndex >= startMeasureIndex && (e.measureIndex > startMeasureIndex || e.quant >= startQuant)
+      const secondsPerBeat = 60 / bpm;
+      const secondsPerQuant = secondsPerBeat / 16;
+      const staves = score.staves || [getActiveStaff(score)];
+      const timeSig = score.timeSignature || "4/4";
+      const firstStaffMeasures = (staves[0] && staves[0].measures) || [];
+      const measureStartTimes = [];
+      let currentGlobalTime = 0;
+      firstStaffMeasures.forEach((measure) => {
+        measureStartTimes.push(currentGlobalTime);
+        let measureQuants;
+        if (measure.isPickup) {
+          measureQuants = measure.events.reduce(
+            (acc, e) => acc + getNoteDuration(e.duration, e.dotted, e.tuplet),
+            0
+          );
+        } else {
+          measureQuants = TIME_SIGNATURES[timeSig] || 64;
+        }
+        currentGlobalTime += measureQuants * secondsPerQuant;
+      });
+      const safeM = Math.min(
+        Math.max(0, startMeasureIndex),
+        Math.max(0, measureStartTimes.length - 1)
       );
-      if (startEvent) {
-        startTimeOffset = startEvent.time;
-        setPlaybackPosition({
-          measureIndex: startEvent.measureIndex,
-          quant: startEvent.quant,
-          duration: startEvent.duration || 0
-        });
-      }
+      const startTimeOffset = (measureStartTimes[safeM] ?? 0) + Math.max(0, startQuant) * secondsPerQuant;
+      setPlaybackPosition({
+        measureIndex: startMeasureIndex,
+        quant: startQuant,
+        duration: 0
+      });
       setIsActive(true);
       requestAnimationFrame(() => {
         requestAnimationFrame(() => {
@@ -3738,10 +3879,12 @@ var usePlayback = (score, bpm) => {
             startTimeOffset,
             (measureIndex, quant, duration) => {
               setPlaybackPosition({ measureIndex, quant, duration: duration || 0 });
+              lastPlayPosition = { measureIndex, quant };
             },
             () => {
               setIsPlaying(false);
               setPlaybackPosition({ measureIndex: null, quant: null, duration: 0 });
+              lastPlayPosition = { measureIndex: 0, quant: 0 };
             }
           );
         });
@@ -3754,9 +3897,23 @@ var usePlayback = (score, bpm) => {
     if (isPlaying2) {
       pausePlayback();
     } else {
-      const resumeMeasure = (_a = playbackPosition.measureIndex) != null ? _a : 0;
-      const resumeQuant = ((_b = playbackPosition.quant) != null ? _b : -1) + 1;
-      playScore(resumeMeasure, resumeQuant);
+      const _pending = consumeHfRiffScorePendingPlay();
+      if (_pending) {
+        playScore(_pending.measureIndex, _pending.quant);
+      } else {
+        const pm = (_a = playbackPosition.measureIndex) != null ? _a : null;
+        const pq = (_b = playbackPosition.quant) != null ? _b : null;
+        let startM;
+        let startQ;
+        if (pm != null && pq != null) {
+          startM = pm;
+          startQ = pq + 1;
+        } else {
+          startM = lastPlayPosition.measureIndex;
+          startQ = lastPlayPosition.quant;
+        }
+        playScore(startM, startQ);
+      }
     }
   }, [isPlaying2, playScore, pausePlayback, playbackPosition]);
   return {
@@ -7158,21 +7315,10 @@ var useModifiers = ({
     if (tools.activeTie !== targetState) tools.handleTieToggle();
   }, [selection, tools, dispatch, scoreRef]);
   const checkDurationValidity = React2.useCallback(
-    (targetDuration) => {
-      if (selection.measureIndex === null || !selection.eventId) return true;
-      const staffIdx = selection.staffIndex !== void 0 ? selection.staffIndex : 0;
-      const staff = scoreRef.current.staves[staffIdx];
-      if (!staff) return true;
-      const measure = staff.measures[selection.measureIndex];
-      if (!measure) return true;
-      return canModifyEventDuration(
-        measure.events,
-        selection.eventId,
-        targetDuration,
-        currentQuantsPerMeasure
-      );
+    (_targetDuration) => {
+      return true;
     },
-    [selection, currentQuantsPerMeasure, scoreRef]
+    []
   );
   const checkDotValidity = React2.useCallback(() => {
     if (selection.measureIndex === null || !selection.eventId) return true;
@@ -8740,13 +8886,36 @@ var useInteraction = ({
 };
 
 // src/hooks/handlers/handlePlayback.ts
+var consumeHfRiffScorePendingPlay = () => {
+  const _g = typeof globalThis !== "undefined" ? globalThis : void 0;
+  const _hf = _g && _g.__HF_RIFFSCORE_PLAY_FROM;
+  if (
+    _hf != null &&
+    typeof _hf === "object" &&
+    typeof _hf.measureIndex === "number" &&
+    Number.isFinite(_hf.measureIndex) &&
+    _hf.measureIndex >= 0
+  ) {
+    const _hm = _hf.measureIndex;
+    const _hq =
+      typeof _hf.quant === "number" && Number.isFinite(_hf.quant)
+        ? _hf.quant
+        : 0;
+    _g.__HF_RIFFSCORE_PLAY_FROM = null;
+    return { measureIndex: _hm, quant: _hq };
+  }
+  return null;
+};
 var handlePlayback = (e, playback, selection, score) => {
   var _a;
   const { playScore, isPlaying: isPlaying2, lastPlayStart } = playback;
   const measures = getActiveStaff(score).measures;
   if (e.key.toLowerCase() === "p") {
     e.preventDefault();
-    if (selection.measureIndex !== null && selection.eventId) {
+    const _pending = consumeHfRiffScorePendingPlay();
+    if (_pending) {
+      playScore(_pending.measureIndex, _pending.quant);
+    } else if (selection.measureIndex !== null && selection.eventId) {
       const m = measures[selection.measureIndex];
       const quant = getEventQuant(m, selection.eventId);
       playScore(selection.measureIndex, quant);
@@ -8765,7 +8934,10 @@ var handlePlayback = (e, playback, selection, score) => {
       if (isPlaying2) {
         playback.pausePlayback();
       } else {
-        if (playback.playbackPosition && playback.playbackPosition.measureIndex !== null) {
+        const _pending = consumeHfRiffScorePendingPlay();
+        if (_pending) {
+          playScore(_pending.measureIndex, _pending.quant);
+        } else if (playback.playbackPosition && playback.playbackPosition.measureIndex !== null) {
           const resumeQuant = ((_a = playback.playbackPosition.quant) != null ? _a : -1) + 1;
           playScore(playback.playbackPosition.measureIndex, resumeQuant);
         } else if (selection.measureIndex !== null && selection.eventId) {
@@ -18041,7 +18213,8 @@ var Toolbar = React2.forwardRef(
     selectedInstrument,
     onInstrumentChange,
     samplerLoaded,
-    onEscape
+    onEscape,
+    toolbarPlugins = []
   }, ref) => {
     var _a, _b, _c;
     const { theme: _theme } = useTheme();
@@ -18190,6 +18363,26 @@ var Toolbar = React2.forwardRef(
                 }
               )
             ] }),
+            toolbarPlugins.length > 0 && /* @__PURE__ */ jsxRuntime.jsx(Divider_default, {}),
+            toolbarPlugins.length > 0 && toolbarPlugins.map((plugin, idx) => /* @__PURE__ */ jsxRuntime.jsx(
+              ToolbarButton_default,
+              {
+                onClick: plugin.onClick,
+                label: plugin.label || `Plugin ${idx + 1}`,
+                title: plugin.title || plugin.label || `Plugin ${idx + 1}`,
+                icon: plugin.icon || /* @__PURE__ */ jsxRuntime.jsx(lucideReact.Settings, { size: 16 }),
+                showLabel: !!plugin.showLabel,
+                isActive: !!plugin.isActive,
+                disabled: !!plugin.disabled,
+                isEmphasized: !!plugin.isEmphasized,
+                isDashed: !!plugin.isDashed,
+                className: plugin.className || "",
+                preventFocus: true,
+                height: TOP_ROW_HEIGHT,
+                variant: "ghost"
+              },
+              plugin.id || `plugin-${idx}`
+            )),
             /* @__PURE__ */ jsxRuntime.jsx("div", { className: "riff-Toolbar__spacer" }),
             /* @__PURE__ */ jsxRuntime.jsx(
               ToolbarButton_default,
@@ -23943,7 +24136,8 @@ var ScoreEditorContent = ({
   showBackground = true,
   showScoreTitle = true,
   enableKeyboard = true,
-  enablePlayback = true
+  enablePlayback = true,
+  toolbarPlugins = []
 }) => {
   const { theme } = useTheme();
   const scoreLogic = useScoreContext();
@@ -24159,7 +24353,8 @@ var ScoreEditorContent = ({
             samplerLoaded,
             errorMsg,
             onToggleHelp: () => setShowHelp(true),
-            onEscape: handleEscape
+            onEscape: handleEscape,
+            toolbarPlugins
           }
         ),
         showHelp && /* @__PURE__ */ jsxRuntime.jsx(Portal_default, { children: /* @__PURE__ */ jsxRuntime.jsx(ShortcutsOverlay_default, { onClose: () => setShowHelp(false) }) }),
@@ -24272,7 +24467,8 @@ var RiffScoreInner = ({ id, config: userConfig }) => {
             showToolbar: config.ui.showToolbar,
             showBackground: config.ui.showBackground,
             enableKeyboard: config.interaction.enableKeyboard,
-            enablePlayback: config.interaction.enablePlayback
+            enablePlayback: config.interaction.enablePlayback,
+            toolbarPlugins: config.ui.toolbarPlugins || []
           }
         ) }) })
       ]
diff --git a/node_modules/riffscore/dist/index.mjs b/node_modules/riffscore/dist/index.mjs
index fc71835..bb8f10b 100644
--- a/node_modules/riffscore/dist/index.mjs
+++ b/node_modules/riffscore/dist/index.mjs
@@ -1159,12 +1159,25 @@ var CLEF_REFERENCE = {
 var getOffsetForPitch = (pitch, clef = "treble") => {
   if (!pitch) return 0;
   const normalizedPitch = getStaffPitch(pitch);
+  const ref = CLEF_REFERENCE[clef] || CLEF_REFERENCE.treble;
+  const match = normalizedPitch.match(/^([A-G])(\d+)$/);
+  if (clef === "alto" || clef === "tenor") {
+    if (!match) return 0;
+    const [, letter, octStr] = match;
+    const octave = parseInt(octStr, 10);
+    const refMatch = ref.pitch.match(/^([A-G])(\d+)$/);
+    if (!refMatch) return ref.offset;
+    const [, refLetter, refOctStr] = refMatch;
+    const refOctave = parseInt(refOctStr, 10);
+    const letterIdx = STAFF_LETTERS.indexOf(letter);
+    const refLetterIdx = STAFF_LETTERS.indexOf(refLetter);
+    const stepsFromRef = (octave - refOctave) * 7 + (letterIdx - refLetterIdx);
+    return ref.offset - stepsFromRef * 6;
+  }
   const mapping = getPitchToOffset(clef);
   if (mapping[normalizedPitch] !== void 0) {
     return mapping[normalizedPitch];
   }
-  const ref = CLEF_REFERENCE[clef] || CLEF_REFERENCE.treble;
-  const match = normalizedPitch.match(/^([A-G])(\d+)$/);
   if (!match) return 0;
   const [, letter, octStr] = match;
   const octave = parseInt(octStr, 10);
@@ -1178,6 +1191,12 @@ var getOffsetForPitch = (pitch, clef = "treble") => {
   return ref.offset - stepsFromRef * 6;
 };
 var getPitchForOffset = (offset, clef = "treble") => {
+  if (clef === "alto" || clef === "tenor") {
+    const ref = CLEF_REFERENCE[clef];
+    if (!ref) return "C4";
+    const stepsFromRef = Math.round((ref.offset - offset) / 6);
+    return movePitchVisual(ref.pitch, stepsFromRef);
+  }
   const mapping = getYToPitch(clef);
   return mapping[offset];
 };
@@ -3486,9 +3505,24 @@ var scheduleTonePlayback = async (timeline, bpm, startTimeOffset = 0, onPosition
   }
   stopTonePlayback();
   Tone.Transport.bpm.value = bpm;
-  const filteredTimeline = timeline.filter((e) => e.time >= startTimeOffset);
+  const filteredTimeline = timeline.filter((e) => e.time >= startTimeOffset - 1e-6);
+  const totalEnd = timeline.reduce(
+    (max, e) => Math.max(max, e.time + (e.duration || 0)),
+    0
+  );
+  const playDuration = Math.max(0, totalEnd - startTimeOffset);
   if (filteredTimeline.length === 0) {
-    onComplete == null ? void 0 : onComplete();
+    if (playDuration > 0) {
+      Tone.Transport.scheduleOnce(() => {
+        updateState({ isPlaying: false });
+        clearHfPlaybackPositionIfInstalled();
+        onComplete == null ? void 0 : onComplete();
+      }, playDuration + 0.1);
+      Tone.Transport.start();
+      updateState({ isPlaying: true });
+    } else {
+      onComplete == null ? void 0 : onComplete();
+    }
     return;
   }
   const adjustedTimeline = filteredTimeline.map((e) => __spreadProps(__spreadValues({}, e), {
@@ -3496,12 +3530,20 @@ var scheduleTonePlayback = async (timeline, bpm, startTimeOffset = 0, onPosition
   }));
   const events = adjustedTimeline.map((e) => ({
     time: e.time,
-    note: e.pitch || freqToNote(e.frequency),
+    type: e.type || "note",
+    note: e.type === "rest" ? null : e.pitch || freqToNote(e.frequency),
     duration: e.duration,
     measureIndex: e.measureIndex,
     quant: e.quant
   }));
   const part = new Tone.Part((time, event) => {
+    notifyHfPlaybackPositionIfInstalled(event.measureIndex, event.quant, event.duration);
+    if (event.type === "rest" || !event.note) {
+      Tone.Draw.schedule(() => {
+        onPositionUpdate == null ? void 0 : onPositionUpdate(event.measureIndex, event.quant, event.duration);
+      }, time);
+      return;
+    }
     instrument.triggerAttackRelease(event.note, event.duration, time);
     Tone.Draw.schedule(() => {
       onPositionUpdate == null ? void 0 : onPositionUpdate(event.measureIndex, event.quant, event.duration);
@@ -3510,9 +3552,10 @@ var scheduleTonePlayback = async (timeline, bpm, startTimeOffset = 0, onPosition
   currentPart = part;
   part.start(0);
   const lastEvent = events[events.length - 1];
-  const endTime = lastEvent.time + lastEvent.duration + 0.1;
+  const endTime = Math.max(lastEvent.time + lastEvent.duration, playDuration) + 0.1;
   Tone.Transport.scheduleOnce(() => {
     updateState({ isPlaying: false });
+    clearHfPlaybackPositionIfInstalled();
     onComplete == null ? void 0 : onComplete();
   }, endTime);
   Tone.Transport.start();
@@ -3541,8 +3584,48 @@ var scheduleChordPlayback = async (chordEvents, startTimeOffset = 0) => {
   chordPart = chordPartInstance;
   chordPartInstance.start(0);
 };
+var scheduleHfMetronomeIfInstalled = (Tone, bpm, startTimeOffset, totalEnd, timeSignature) => {
+  const g = typeof globalThis !== "undefined" ? globalThis : void 0;
+  const schedule = g && g.__HF_SCHEDULE_METRONOME;
+  if (typeof schedule === "function" && Tone) {
+    schedule(Tone, { bpm, startTimeOffset, totalEnd, timeSignature });
+  }
+};
+var clearHfMetronomeIfInstalled = (Tone) => {
+  const g = typeof globalThis !== "undefined" ? globalThis : void 0;
+  const clear = g && g.__HF_CLEAR_METRONOME;
+  if (typeof clear === "function" && Tone) {
+    clear(Tone);
+  }
+};
+var notifyHfPlaybackPositionIfInstalled = (measureIndex, quant, durationSec) => {
+  const g = typeof globalThis !== "undefined" ? globalThis : void 0;
+  const notify = g && g.__HF_ON_PLAYBACK_POSITION;
+  if (typeof notify === "function") {
+    notify(measureIndex, quant, durationSec);
+  }
+};
+var clearHfPlaybackPositionIfInstalled = () => {
+  const g = typeof globalThis !== "undefined" ? globalThis : void 0;
+  const clear = g && g.__HF_CLEAR_PLAYBACK_POSITION;
+  if (typeof clear === "function") {
+    clear();
+  }
+};
 var scheduleScorePlayback = async (timeline, score, bpm, chordConfig, startTimeOffset = 0, onPositionUpdate, onComplete) => {
+  const totalEnd = timeline.reduce(
+    (max, e) => Math.max(max, e.time + (e.duration || 0)),
+    0
+  );
   await scheduleTonePlayback(timeline, bpm, startTimeOffset, onPositionUpdate, onComplete);
+  const Tone = await loadTone();
+  scheduleHfMetronomeIfInstalled(
+    Tone,
+    bpm,
+    startTimeOffset,
+    totalEnd,
+    score == null ? void 0 : score.timeSignature
+  );
   if ((chordConfig == null ? void 0 : chordConfig.enabled) && score.chordTrack && score.chordTrack.length > 0) {
     const chordEvents = createChordPlaybackEvents(score, bpm, chordConfig.velocity);
     await scheduleChordPlayback(chordEvents, startTimeOffset);
@@ -3551,6 +3634,8 @@ var scheduleScorePlayback = async (timeline, score, bpm, chordConfig, startTimeO
 var stopTonePlayback = () => {
   const Tone = getTone();
   if (!Tone) return;
+  clearHfMetronomeIfInstalled(Tone);
+  clearHfPlaybackPositionIfInstalled();
   Tone.Transport.stop();
   Tone.Transport.cancel();
   if (currentPart) {
@@ -3607,19 +3692,15 @@ var createTimeline = (score, bpm) => {
       let currentMeasureQuant = 0;
       measure.events.forEach((event, eIndex) => {
         const eventDurQuants = getNoteDuration(event.duration, event.dotted, event.tuplet);
+        const eventTime = measureStartTime + currentMeasureQuant * secondsPerQuant;
+        let hadPitch = false;
         event.notes.forEach((note) => {
           if (!note.pitch) {
-            if (process.env.NODE_ENV !== "production") {
-              console.warn("TimelineService: Note without pitch detected", {
-                measureIndex: mIndex,
-                eventIndex: eIndex,
-                note
-              });
-            }
             return;
           }
+          hadPitch = true;
           rawEvents.push({
-            time: measureStartTime + currentMeasureQuant * secondsPerQuant,
+            time: eventTime,
             duration: eventDurQuants * secondsPerQuant,
             pitch: note.pitch,
             tied: !!note.tied,
@@ -3629,10 +3710,25 @@ var createTimeline = (score, bpm) => {
             staffIndex
           });
         });
+        if (!hadPitch && eventDurQuants > 0) {
+          rawEvents.push({
+            time: eventTime,
+            duration: eventDurQuants * secondsPerQuant,
+            pitch: null,
+            isRest: true,
+            measureIndex: mIndex,
+            eventIndex: eIndex,
+            quant: currentMeasureQuant,
+            staffIndex
+          });
+        }
         currentMeasureQuant += eventDurQuants;
       });
     });
     rawEvents.sort((a, b) => {
+      if (a.isRest && b.isRest) return a.time - b.time;
+      if (a.isRest) return -1;
+      if (b.isRest) return 1;
       if (a.pitch !== b.pitch) return a.pitch.localeCompare(b.pitch);
       return a.time - b.time;
     });
@@ -3640,7 +3736,7 @@ var createTimeline = (score, bpm) => {
       let current = rawEvents[0];
       for (let i = 1; i < rawEvents.length; i++) {
         const next = rawEvents[i];
-        const isConnected = current.tied && next.pitch === current.pitch && Math.abs(next.time - (current.time + current.duration)) < 1e-3;
+        const isConnected = !current.isRest && !next.isRest && current.tied && next.pitch === current.pitch && Math.abs(next.time - (current.time + current.duration)) < 1e-3;
         if (isConnected) {
           current.duration += next.duration;
           current.tied = next.tied;
@@ -3653,6 +3749,18 @@ var createTimeline = (score, bpm) => {
     }
   });
   function addEventToTimeline(raw) {
+    if (raw.isRest) {
+      timeline.push({
+        time: raw.time,
+        duration: raw.duration,
+        type: "rest",
+        measureIndex: raw.measureIndex,
+        eventIndex: raw.eventIndex,
+        staffIndex: raw.staffIndex,
+        quant: raw.quant
+      });
+      return;
+    }
     const freq = getFrequency(raw.pitch);
     if (freq) {
       timeline.push({
@@ -3686,6 +3794,16 @@ var usePlayback = (score, bpm) => {
     });
     isInitialized2.current = true;
   }, []);
+  useEffect(() => {
+    const ev = "riffscore-clear-playback-anchor";
+    const handler = () => {
+      setPlaybackPosition({ measureIndex: null, quant: null, duration: 0 });
+    };
+    if (typeof window !== "undefined") {
+      window.addEventListener(ev, handler);
+      return () => window.removeEventListener(ev, handler);
+    }
+  }, []);
   const exitPlaybackMode = useCallback(() => {
     setIsActive(false);
   }, []);
@@ -3694,6 +3812,10 @@ var usePlayback = (score, bpm) => {
     setIsPlaying(false);
     setIsActive(true);
     setPlaybackPosition({ measureIndex: null, quant: null, duration: 0 });
+    lastPlayPosition = { measureIndex: 0, quant: 0 };
+    if (typeof globalThis !== "undefined") {
+      globalThis.__HF_RIFFSCORE_PLAY_FROM = null;
+    }
   }, []);
   const pausePlayback = useCallback(() => {
     stopTonePlayback();
@@ -3702,24 +3824,43 @@ var usePlayback = (score, bpm) => {
   }, []);
   const playScore = useCallback(
     async (startMeasureIndex = 0, startQuant = 0) => {
+      lastPlayPosition = { measureIndex: startMeasureIndex, quant: startQuant };
       await ensureInit();
       stopTonePlayback();
       setLastPlayStart({ measureIndex: startMeasureIndex, quant: startQuant });
       setIsPlaying(true);
       setIsActive(true);
       const timeline = createTimeline(score, bpm);
-      let startTimeOffset = 0;
-      const startEvent = timeline.find(
-        (e) => e.measureIndex >= startMeasureIndex && (e.measureIndex > startMeasureIndex || e.quant >= startQuant)
+      const secondsPerBeat = 60 / bpm;
+      const secondsPerQuant = secondsPerBeat / 16;
+      const staves = score.staves || [getActiveStaff(score)];
+      const timeSig = score.timeSignature || "4/4";
+      const firstStaffMeasures = (staves[0] && staves[0].measures) || [];
+      const measureStartTimes = [];
+      let currentGlobalTime = 0;
+      firstStaffMeasures.forEach((measure) => {
+        measureStartTimes.push(currentGlobalTime);
+        let measureQuants;
+        if (measure.isPickup) {
+          measureQuants = measure.events.reduce(
+            (acc, e) => acc + getNoteDuration(e.duration, e.dotted, e.tuplet),
+            0
+          );
+        } else {
+          measureQuants = TIME_SIGNATURES[timeSig] || 64;
+        }
+        currentGlobalTime += measureQuants * secondsPerQuant;
+      });
+      const safeM = Math.min(
+        Math.max(0, startMeasureIndex),
+        Math.max(0, measureStartTimes.length - 1)
       );
-      if (startEvent) {
-        startTimeOffset = startEvent.time;
-        setPlaybackPosition({
-          measureIndex: startEvent.measureIndex,
-          quant: startEvent.quant,
-          duration: startEvent.duration || 0
-        });
-      }
+      const startTimeOffset = (measureStartTimes[safeM] ?? 0) + Math.max(0, startQuant) * secondsPerQuant;
+      setPlaybackPosition({
+        measureIndex: startMeasureIndex,
+        quant: startQuant,
+        duration: 0
+      });
       setIsActive(true);
       requestAnimationFrame(() => {
         requestAnimationFrame(() => {
@@ -3732,10 +3873,12 @@ var usePlayback = (score, bpm) => {
             startTimeOffset,
             (measureIndex, quant, duration) => {
               setPlaybackPosition({ measureIndex, quant, duration: duration || 0 });
+              lastPlayPosition = { measureIndex, quant };
             },
             () => {
               setIsPlaying(false);
               setPlaybackPosition({ measureIndex: null, quant: null, duration: 0 });
+              lastPlayPosition = { measureIndex: 0, quant: 0 };
             }
           );
         });
@@ -3748,9 +3891,23 @@ var usePlayback = (score, bpm) => {
     if (isPlaying2) {
       pausePlayback();
     } else {
-      const resumeMeasure = (_a = playbackPosition.measureIndex) != null ? _a : 0;
-      const resumeQuant = ((_b = playbackPosition.quant) != null ? _b : -1) + 1;
-      playScore(resumeMeasure, resumeQuant);
+      const _pending = consumeHfRiffScorePendingPlay();
+      if (_pending) {
+        playScore(_pending.measureIndex, _pending.quant);
+      } else {
+        const pm = (_a = playbackPosition.measureIndex) != null ? _a : null;
+        const pq = (_b = playbackPosition.quant) != null ? _b : null;
+        let startM;
+        let startQ;
+        if (pm != null && pq != null) {
+          startM = pm;
+          startQ = pq + 1;
+        } else {
+          startM = lastPlayPosition.measureIndex;
+          startQ = lastPlayPosition.quant;
+        }
+        playScore(startM, startQ);
+      }
     }
   }, [isPlaying2, playScore, pausePlayback, playbackPosition]);
   return {
@@ -7152,21 +7309,10 @@ var useModifiers = ({
     if (tools.activeTie !== targetState) tools.handleTieToggle();
   }, [selection, tools, dispatch, scoreRef]);
   const checkDurationValidity = useCallback(
-    (targetDuration) => {
-      if (selection.measureIndex === null || !selection.eventId) return true;
-      const staffIdx = selection.staffIndex !== void 0 ? selection.staffIndex : 0;
-      const staff = scoreRef.current.staves[staffIdx];
-      if (!staff) return true;
-      const measure = staff.measures[selection.measureIndex];
-      if (!measure) return true;
-      return canModifyEventDuration(
-        measure.events,
-        selection.eventId,
-        targetDuration,
-        currentQuantsPerMeasure
-      );
+    (_targetDuration) => {
+      return true;
     },
-    [selection, currentQuantsPerMeasure, scoreRef]
+    []
   );
   const checkDotValidity = useCallback(() => {
     if (selection.measureIndex === null || !selection.eventId) return true;
@@ -8734,13 +8880,36 @@ var useInteraction = ({
 };
 
 // src/hooks/handlers/handlePlayback.ts
+var consumeHfRiffScorePendingPlay = () => {
+  const _g = typeof globalThis !== "undefined" ? globalThis : void 0;
+  const _hf = _g && _g.__HF_RIFFSCORE_PLAY_FROM;
+  if (
+    _hf != null &&
+    typeof _hf === "object" &&
+    typeof _hf.measureIndex === "number" &&
+    Number.isFinite(_hf.measureIndex) &&
+    _hf.measureIndex >= 0
+  ) {
+    const _hm = _hf.measureIndex;
+    const _hq =
+      typeof _hf.quant === "number" && Number.isFinite(_hf.quant)
+        ? _hf.quant
+        : 0;
+    _g.__HF_RIFFSCORE_PLAY_FROM = null;
+    return { measureIndex: _hm, quant: _hq };
+  }
+  return null;
+};
 var handlePlayback = (e, playback, selection, score) => {
   var _a;
   const { playScore, isPlaying: isPlaying2, lastPlayStart } = playback;
   const measures = getActiveStaff(score).measures;
   if (e.key.toLowerCase() === "p") {
     e.preventDefault();
-    if (selection.measureIndex !== null && selection.eventId) {
+    const _pending = consumeHfRiffScorePendingPlay();
+    if (_pending) {
+      playScore(_pending.measureIndex, _pending.quant);
+    } else if (selection.measureIndex !== null && selection.eventId) {
       const m = measures[selection.measureIndex];
       const quant = getEventQuant(m, selection.eventId);
       playScore(selection.measureIndex, quant);
@@ -8759,7 +8928,10 @@ var handlePlayback = (e, playback, selection, score) => {
       if (isPlaying2) {
         playback.pausePlayback();
       } else {
-        if (playback.playbackPosition && playback.playbackPosition.measureIndex !== null) {
+        const _pending = consumeHfRiffScorePendingPlay();
+        if (_pending) {
+          playScore(_pending.measureIndex, _pending.quant);
+        } else if (playback.playbackPosition && playback.playbackPosition.measureIndex !== null) {
           const resumeQuant = ((_a = playback.playbackPosition.quant) != null ? _a : -1) + 1;
           playScore(playback.playbackPosition.measureIndex, resumeQuant);
         } else if (selection.measureIndex !== null && selection.eventId) {
@@ -18035,7 +18207,8 @@ var Toolbar = forwardRef(
     selectedInstrument,
     onInstrumentChange,
     samplerLoaded,
-    onEscape
+    onEscape,
+    toolbarPlugins = []
   }, ref) => {
     var _a, _b, _c;
     const { theme: _theme } = useTheme();
@@ -18184,6 +18357,26 @@ var Toolbar = forwardRef(
                 }
               )
             ] }),
+            toolbarPlugins.length > 0 && /* @__PURE__ */ jsx(Divider_default, {}),
+            toolbarPlugins.length > 0 && toolbarPlugins.map((plugin, idx) => /* @__PURE__ */ jsx(
+              ToolbarButton_default,
+              {
+                onClick: plugin.onClick,
+                label: plugin.label || `Plugin ${idx + 1}`,
+                title: plugin.title || plugin.label || `Plugin ${idx + 1}`,
+                icon: plugin.icon || /* @__PURE__ */ jsx(Settings, { size: 16 }),
+                showLabel: !!plugin.showLabel,
+                isActive: !!plugin.isActive,
+                disabled: !!plugin.disabled,
+                isEmphasized: !!plugin.isEmphasized,
+                isDashed: !!plugin.isDashed,
+                className: plugin.className || "",
+                preventFocus: true,
+                height: TOP_ROW_HEIGHT,
+                variant: "ghost"
+              },
+              plugin.id || `plugin-${idx}`
+            )),
             /* @__PURE__ */ jsx("div", { className: "riff-Toolbar__spacer" }),
             /* @__PURE__ */ jsx(
               ToolbarButton_default,
@@ -23937,7 +24130,8 @@ var ScoreEditorContent = ({
   showBackground = true,
   showScoreTitle = true,
   enableKeyboard = true,
-  enablePlayback = true
+  enablePlayback = true,
+  toolbarPlugins = []
 }) => {
   const { theme } = useTheme();
   const scoreLogic = useScoreContext();
@@ -24153,7 +24347,8 @@ var ScoreEditorContent = ({
             samplerLoaded,
             errorMsg,
             onToggleHelp: () => setShowHelp(true),
-            onEscape: handleEscape
+            onEscape: handleEscape,
+            toolbarPlugins
           }
         ),
         showHelp && /* @__PURE__ */ jsx(Portal_default, { children: /* @__PURE__ */ jsx(ShortcutsOverlay_default, { onClose: () => setShowHelp(false) }) }),
@@ -24266,7 +24461,8 @@ var RiffScoreInner = ({ id, config: userConfig }) => {
             showToolbar: config.ui.showToolbar,
             showBackground: config.ui.showBackground,
             enableKeyboard: config.interaction.enableKeyboard,
-            enablePlayback: config.interaction.enablePlayback
+            enablePlayback: config.interaction.enablePlayback,
+            toolbarPlugins: config.ui.toolbarPlugins || []
           }
         ) }) })
       ]
