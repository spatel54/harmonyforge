# HarmonyForge — PDF → MusicXML uses Audiveris (Java 25+), not Python/oemer.
#
# Local setup:  make audiveris-setup
# Manual QA:    make audiveris-convert  (PDFs in scripts/audiveris/input/)
# Docker:       make docker-build       (bundles Audiveris in the image)
#
# Node app deps: cd frontend && npm install   (or `make install` from repo root)
